import PurchaseOrderActionButtons from './PurchaseOrderActionButtons.vue'

// `btn` is a component value, so it lives beside the component it mounts.
export default {
  btn: PurchaseOrderActionButtons
}
