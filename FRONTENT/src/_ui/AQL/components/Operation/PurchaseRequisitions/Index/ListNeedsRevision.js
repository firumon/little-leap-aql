import RequisitionActionButtons from './RequisitionActionButtons.vue'

// `btn` is a component value, so it lives beside the component it mounts. Every other
// prop for this view is a `Props<Identity>` block in the page contract.
export default {
  btn: RequisitionActionButtons
}
