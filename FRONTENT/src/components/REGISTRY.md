# Frontend Component Registry

Purpose: quick discovery of reusable UI blocks under `FRONTENT/src/components/`.

Rule: update this file whenever a new reusable component is added or a listed component's API changes.

Design principle:
- Prefer tiny, single-responsibility reusable components.
- Avoid page-private one-off wrappers unless there is a clear reuse path.

| Component Name | Description | Props | Events | Path |
|---|---|---|---|---|
| `MasterListHeader` | Master list header with title/subtitle, sync status, and record counters. | `{ config: Object, filteredCount: Number, totalCount: Number, loading: Boolean, backgroundSyncing: Boolean }` | `reload()` | `FRONTENT/src/components/Masters/MasterListHeader.vue` |
| `MasterListReportBar` | Toolbar-level report actions rendered as a standalone section card; hidden when no toolbar reports exist. | `{ reports: Array, isGenerating: Boolean }` | `generate-report(report)` | `FRONTENT/src/components/Masters/MasterListReportBar.vue` |
| `MasterListToolbar` | Search input for master list filtering. | `{ searchTerm: String }` | `update:searchTerm(value)` | `FRONTENT/src/components/Masters/MasterListToolbar.vue` |
| `MasterListRecords` | Card-list section with loading/empty states, field-based title/subtitle resolution, and child count badges per record. | `{ items: Array, loading: Boolean, resolvedFields: Array, childCountMap: Object }` | `navigate-to-view(row)` | `FRONTENT/src/components/Masters/MasterListRecords.vue` |
| `MasterListRecordsRecord` | Individual master record card with click-to-open detail (no status badge). | `{ row: Object, resolvePrimaryText: Function, resolveSecondaryText: Function }` | `open-detail(row)` | `FRONTENT/src/components/Masters/_common/MasterListRecordsRecord.vue` |
| `MasterListRecordsLoading` | Loading spinner for the master list. | `{}` | `None` | `FRONTENT/src/components/Masters/_common/MasterListRecordsLoading.vue` |
| `MasterListRecordsEmpty` | Empty state for the master list. | `{}` | `None` | `FRONTENT/src/components/Masters/_common/MasterListRecordsEmpty.vue` |
| `MasterListViewSwitcher` | Filter view chip bar for switching between named list views (Active/Inactive, custom filters). Shows counts per view, outlined/filled toggle for active state. Hidden when no views configured. | `{ views: Array, activeViewName: String, counts: Object }` | `update:activeViewName(name)` | `FRONTENT/src/components/Masters/MasterListViewSwitcher.vue` |
| `ResourceBreadcrumb` | Breadcrumb navigation for resource sub-route pages (Resource > Code > Action). Shared across masters, operations, and accounts scopes. | `{ scope: String, resourceSlug: String, resourceTitle: String, code: String, action: String, actionLabel: String }` |  | `FRONTENT/src/components/Masters/_common/ResourceBreadcrumb.vue` |
| `ReloadButton` | Shared resource-aware refresh button. Resolves current, parent, child, and `*Code` column dependencies and refreshes them through the resource IO sync flow. | `{}` | `None` | `FRONTENT/src/components/shared/ReloadButton.vue` |
| `AppDate` | Reusable Quasar date input with a popup calendar, configured to read/write native HTML `YYYY-MM-DD` formatted values. | `{ modelValue: String }` | `update:modelValue(value)` | `FRONTENT/src/components/shared/AppDate.vue` |
| `OutletVisitActionDialog` | Reusable bottom-sheet dialog for complete/postpone/cancel actions on a planned outlet visit. Emits `{ action, fields }` so the caller can drive the matching composable function. | `{ modelValue: Boolean, visit: Object, outletLabel: String, saving: Boolean }` | `update:modelValue(value)`, `confirm({ action, fields })`, `cancel` | `FRONTENT/src/components/Operations/Outlets/OutletVisitActionDialog.vue` |
| `SectionDividerLabel` | Centered text overline between two horizontal rules, used to title mid-page sections (e.g. PRIORITY, HISTORY, VISITS). | `{ label: String }` | — | `FRONTENT/src/components/shared/SectionDividerLabel.vue` |
| `ChildRecordsTable` | Inline editable table for child resource records within Add/Edit/View pages. | `{ childResource: Object, records: Array, headers: Array, readonly: Boolean }` | `add`, `remove(index)`, `update(index, field, value)` | `FRONTENT/src/components/Masters/ChildRecordsTable.vue` |
| `BulkUploadControlsCard` | Left-side bulk upload controls: resource select, header input, file upload, and plotting. | `{ selectedResourceName: String, resourceOptions: Array, headersDisplay: String, rawContent: String, csvFile: Object }` | `update:selectedResourceName(value)`, `update:headersDisplay(value)`, `update:rawContent(value)`, `update:csvFile(file)`, `resourceSelected(value)`, `fileUpload(file)`, `downloadTemplate()`, `plotTable()` | `FRONTENT/src/components/Masters/BulkUpload/BulkUploadControlsCard.vue` |
| `BulkUploadPreviewTable` | Bulk upload preview/edit table with per-row inline editing and upload actions. | `{ rows: Array, columns: Array, isUploading: Boolean }` | `clearAll()`, `uploadAll()`, `deleteRow(row)`, `cellEdited(row, field, value)` | `FRONTENT/src/components/Masters/BulkUpload/BulkUploadPreviewTable.vue` |
| `BulkUploadEmptyState` | Empty-state panel for bulk upload page before plotting rows. | `{}` | `None` | `FRONTENT/src/components/Masters/BulkUpload/BulkUploadEmptyState.vue` |
| `ReportInputDialog` | Dynamic form dialog for collecting user inputs before PDF report generation. Renders text/number/date/boolean/select fields based on report config. | `{ modelValue: Boolean, report: Object, formValues: Object, isGenerating: Boolean }` | `update:modelValue(value)`, `update:formValues(values)`, `confirm()`, `cancel()` | `FRONTENT/src/components/Masters/ReportInputDialog.vue` |
| `MasterViewHeader` | View page header with record code, primary text title, and status badge. | `{ config, record, resourceName, code, primaryText }` | — | `FRONTENT/src/components/Masters/MasterViewHeader.vue` |
| `MasterViewActionBar` | View page action buttons: edit, additional actions, record-level reports. | `{ config, record, code, resourceSlug, scope, additionalActions, permissions }` | — | `FRONTENT/src/components/Masters/MasterViewActionBar.vue` |
| `MasterViewDetails` | Key-value detail grid for record fields on the View page. | `{ record, resolvedFields, resourceHeaders }` | — | `FRONTENT/src/components/Masters/MasterViewDetails.vue` |
| `MasterViewAudit` | Created/Updated audit timestamps on the View page. | `{ record }` | — | `FRONTENT/src/components/Masters/MasterViewAudit.vue` |
| `MasterViewChildren` | Child resource record tables on the View page. | `{ childResources, record, code }` | — | `FRONTENT/src/components/Masters/MasterViewChildren.vue` |
| `MasterViewChild` | Single child resource section on the View page. | `{ childResource: Object, childRecords: Array, additionalActions: Array }` | `view-child(childResource, code)` | `FRONTENT/src/components/Masters/_common/MasterViewChild.vue` |
| `MasterViewLoading` | Loading spinner for the master view page. | `{}` | `None` | `FRONTENT/src/components/Masters/_common/MasterViewLoading.vue` |
| `MasterViewEmpty` | Empty state for the master view page. | `{}` | `back` | `FRONTENT/src/components/Masters/_common/MasterViewEmpty.vue` |
| `WarehouseStockRows` | UI-only warehouse stock renderer with desktop table and mobile list views. | `{ rows: Array }` | `None` | `FRONTENT/src/components/Masters/Warehouses/WarehouseStockRows.vue` |
| `MasterAddHeader` | Add page header with "Create {ResourceName}" title. | `{ config, resourceName }` | — | `FRONTENT/src/components/Masters/MasterAddHeader.vue` |
| `MasterAddForm` | Add page form fields (status select + text inputs). | `{ resolvedFields, parentForm, resourceHeaders }` | `update:field(header, value)` | `FRONTENT/src/components/Masters/MasterAddForm.vue` |
| `MasterAddChildren` | Add page child records table with inline editing. | `{ childResources, childRecords }` | `add-child(name)`, `remove-child(name, index)`, `update-child-field(name, index, field, value)` | `FRONTENT/src/components/Masters/MasterAddChildren.vue` |
| `MasterAddActions` | Add page cancel/submit action buttons. | `{ saving, submitLabel }` | `cancel()`, `submit()` | `FRONTENT/src/components/Masters/MasterAddActions.vue` |
| `MasterEditHeader` | Edit page header with "Edit {ResourceName} — {Code}" title. | `{ config, resourceName, code }` | — | `FRONTENT/src/components/Masters/MasterEditHeader.vue` |
| `MasterEditForm` | Edit page form with read-only code field + editable fields. | `{ resolvedFields, parentForm, resourceHeaders, code }` | `update:field(header, value)` | `FRONTENT/src/components/Masters/MasterEditForm.vue` |
| `MasterEditChildren` | Edit page child records table with inline editing. | `{ childResources, childRecords }` | `add-child(name)`, `remove-child(name, index)`, `update-child-field(name, index, field, value)` | `FRONTENT/src/components/Masters/MasterEditChildren.vue` |
| `MasterEditActions` | Edit page cancel/submit action buttons. | `{ saving, submitLabel }` | `cancel()`, `submit()` | `FRONTENT/src/components/Masters/MasterEditActions.vue` |
| `MasterActionHeader` | Action page header with action icon, label, and record code. | `{ config, record, code, actionConfig, primaryText }` | — | `FRONTENT/src/components/Masters/MasterActionHeader.vue` |
| `MasterActionForm` | Action page form with outcome selector and dynamic fields. | `{ actionConfig, actionFields, actionForm }` | `update:field(key, value)`, `update:outcome(value)` | `FRONTENT/src/components/Masters/MasterActionForm.vue` |
| `MasterActionActions` | Action page submit/cancel buttons with configurable label/icon/color. | `{ actionConfig, saving, submitDisabled }` | `cancel()`, `submit()` | `FRONTENT/src/components/Masters/MasterActionActions.vue` |
| `StockEntryGrid` | Editable stock register for `/operations/stock-movements/direct-entry`. Loads existing stock for a warehouse, allows inline editing and new rows, handles save deltas. | `{ warehouseCode: String }` | — | `FRONTENT/src/components/Operations/StockMovements/StockEntryGrid.vue` |
| `GrnStockEntryGrid` | Mobile-friendly GRN allocation grid for `/operations/stock-movements/grn-entry`. Displays accepted GRN items, storage splits, current stock, and allocation balance. | `{ items: Array, allocationGroups: Object, summary: Object, storageOptions: Array, currentStockFor: Function }` | `update-allocation(rowId, patch)`, `add-allocation(itemCode)`, `remove-allocation(rowId)` | `FRONTENT/src/components/Operations/StockMovements/GrnStockEntryGrid.vue` |
| `MenuTreeNode` | Recursive sidebar menu node renderer. Renders a `q-expansion-item` (group) or `q-item` (leaf) based on `node.type`. Used by `MainLayout.vue` to build an N-level sidebar tree from `group` path arrays. | `{ node: Object }` (node has `type: 'group'|'leaf'`, `key`, `label/navLabel`, `icon/navIcon`, `children?`, `routePath?`) | — | `FRONTENT/src/components/MenuTreeNode.vue` |
| `PurchaseRequisitionAddItemDialog` | Bottom-sheet add-item dialog used by PR create/review flows for SKU selection and quantity/rate entry. | `{ modelValue, newItem, skuOptions, formatCurrency }` | `update:modelValue(value)`, `filter-skus(...)`, `confirm-add-item()` | `FRONTENT/src/components/Operations/PurchaseRequisitions/PurchaseRequisitionAddItemDialog.vue` |
| `RfqSupplierFlowPage` | Shared RFQ supplier dispatch page body used by the assign-supplier and mark-as-sent record pages. Handles supplier selection, assignment, dispatch marking, and the supporting RFQ summary panels. | `{ mode: String }` | `None` | `FRONTENT/src/components/Operations/Rfqs/RfqSupplierFlowPage.vue` |
| `POReceivingSummaryCards` | UI-only summary cards for PO Receiving totals. | `{ summary: Object }` | `None` | `FRONTENT/src/components/Operations/PoReceivings/POReceivingSummaryCards.vue` |
| `POReceivingItemGrid` | UI-only editable/read-only PO Receiving item grid with scrollable desktop table, mobile list layout, and derived quantity display columns. | `{ items: Array, editable: Boolean }` | `update:item(index, patch)` | `FRONTENT/src/components/Operations/PoReceivings/POReceivingItemGrid.vue` |
| `POReceivingBulkToolbar` | UI-only bulk quantity actions for PO Receiving draft lines. | `{}` | `receive-all()`, `clear()` | `FRONTENT/src/components/Operations/PoReceivings/POReceivingBulkToolbar.vue` |
| `POReceivingReportLinks` | Disabled report placeholder list for future damage/reject/short/excess reports. | `{ reports: Array }` | `None` | `FRONTENT/src/components/Operations/PoReceivings/POReceivingReportLinks.vue` |
| `GoodsReceiptItemsTable` | UI-only accepted-quantity item table for Goods Receipt views. | `{ items: Array }` | `None` | `FRONTENT/src/components/Operations/GoodsReceipts/GoodsReceiptItemsTable.vue` |
| `OutletHeaderPanel` | UI-only summary header card for outlet operation pages with title/subtitle, optional stats, and side slot. | `{ title: String, subtitle: String, stats: Array }` | `None` | `FRONTENT/src/components/Operations/Outlets/OutletHeaderPanel.vue` |
| `OutletItemGrid` | UI-only editable/read-only outlet SKU quantity grid used by restock, delivery, and consumption rows. Supports optional storage column and configurable quantity columns. | `{ items: Array, editable: Boolean, showStorage: Boolean, quantityColumns: Array }` | `update:item(index, patch)`, `remove:item(index)` | `FRONTENT/src/components/Operations/Outlets/OutletItemGrid.vue` |
| `OutletProgressChip` | UI-only chip that maps outlet workflow status/progress values to shared labels/colors. | `{ progress: String }` | `None` | `FRONTENT/src/components/Operations/Outlets/OutletProgressChip.vue` |
| `RestockApprovalView` | UI-only outlet restock approval review with grouped availability sections, workflow comments, recommendation actions, approval, send-back, and rejection. | `{ restock: Object, approvalGroups: Array, hasAllocatedRows: Boolean, outletName: String, approveLoading: Boolean, sendBackLoading: Boolean, rejectLoading: Boolean, formatWorkflowCommentHtml: Function }` | `applyRecommendation(rowKey)`, `updateAllocationLine(payload)`, `addLine(rowKey)`, `removeLine(payload)`, `resetAllocation(rowKey)`, `approve(comment)`, `sendBack(comment)`, `reject(comment)` | `FRONTENT/src/components/Operations/Outlets/RestockApprovalView.vue` |
| `RestockCard` | Outlet restock card with outlet label, date/user line, progress icon, urgency border, and item progress summary. | `{ restock: Object, outletLabel?: String, itemSummary?: String }` | `None` | `FRONTENT/src/components/Operations/Outlets/RestockCard.vue` |
| `RestockDraftView` | Outlet restock edit panel for draft/resubmission flow with draft rows, quantities, and workflow comment actions. | `{ restock: Object, rows: Array, skuOptions: Array, outletName: String, saving: Boolean, formatWorkflowCommentHtml: Function, addRow: Function, updateRow: Function, removeRow: Function }` | `save-draft`, `submit` | `FRONTENT/src/components/Operations/Outlets/RestockDraftView.vue` |
| `RestockReadonlyView` | Outlet restock read-only panel for approved/delivered history with ORSI progress, warehouse, storage, and workflow summary. | `{ restock: Object, rows: Array, skuOptions: Array, outletName: String, formatWorkflowCommentHtml: Function }` | `None` | `FRONTENT/src/components/Operations/Outlets/RestockReadonlyView.vue` |
| `OrsiAllocationRow` | UI-only ORSI allocation request renderer with prepared recommendation/allocation lines, pending remainder display, and line-level intent events. | `{ rowView: Object }` | `applyRecommendation(rowKey)`, `updateAllocationLine(payload)`, `addLine(rowKey)`, `removeLine(payload)`, `resetAllocation(rowKey)` | `FRONTENT/src/components/Operations/Outlets/OrsiAllocationRow.vue` |
| `OutletDeliveryItemRow` | UI-only ODI row with SKU/storage quantity, progress chip, and item delivery action. | `{ row: Object, canDeliver: Boolean }` | `deliver(row)` | `FRONTENT/src/components/Operations/Outlets/OutletDeliveryItemRow.vue` |
| `OutletDeliverySummaryPanel` | Header summary for an OD record with outlet count and delivered/total item counts. | `{ delivery: Object, summary: Object }` | `None` | `FRONTENT/src/components/Operations/Outlets/OutletDeliverySummaryPanel.vue` |
| `RestockSummaryBar` | Outlet restock summary card row with progress counts and role-aware filtering. | `{ counts: Object, permissions: Object }` | `filter(key)` | `FRONTENT/src/components/Operations/Outlets/RestockSummaryBar.vue` |
| `VisitSummaryBar` | Outlet visit summary chip row for priority counts with scroll-to section behavior. | `{ stats: Object }` | `scrollTo(sectionKey)` | `FRONTENT/src/components/Operations/Outlets/VisitSummaryBar.vue` |
| `OutletStockRows` | UI-only outlet stock list with SKU/outlet labels, storage display, quantity badge, empty state, and row selection. | `{ rows: Array, skuLabel: Function, outletLabel: Function }` | `select(row)` | `FRONTENT/src/components/Operations/Outlets/OutletStockRows.vue` |
| `OutletMovementTimeline` | UI-only timeline for outlet movement ledger rows, showing reference type, quantity, date, SKU, storage, and reference code. | `{ movements: Array }` | `None` | `FRONTENT/src/components/Operations/Outlets/OutletMovementTimeline.vue` |
| `OutletConsumptionContextStep` | UI-only outlet/date/user/visit context step for mobile outlet consumption creation. | `{ form: Object, checklist: Object, outletOptions: Array, plannedVisits: Array }` | `outlet-change(value)`, `select-visit(code)`, `update-checklist(patch)` | `FRONTENT/src/components/Operations/Outlets/OutletConsumptionContextStep.vue` |
| `OutletConsumptionStockCountStep` | UI-only mobile stock-count card list with current quantity controls, Same/0 quick actions, sold qty, and variance warning. | `{ rows: Array }` | `update-current(index, value)`, `increment(index)`, `decrement(index)`, `set-zero(index)`, `set-system(index)` | `FRONTENT/src/components/Operations/Outlets/OutletConsumptionStockCountStep.vue` |
| `OutletConsumptionSummaryStep` | UI-only sold/restock summary and checklist row renderer for outlet consumption submission. | `{ soldRows: Array, restockRows: Array, skuOptions: Array, checklist: Object, hasVisit: Boolean }` | `update-restock(index, patch)`, `add-restock()`, `remove-restock(index)`, `update-checklist(patch)` | `FRONTENT/src/components/Operations/Outlets/OutletConsumptionSummaryStep.vue` |
| `OutletConsumptionPendingInvoiceView` | UI-only pending-invoice consumption view with human-readable context and generate-invoice command. | `{ record: Object, items: Array, acting: Boolean, outletName: Function, visitLabel: Function, formatDate: Function }` | `generate-invoice()` | `FRONTENT/src/components/Operations/Outlets/OutletConsumptionPendingInvoiceView.vue` |
| `OutletConsumptionInvoiceGeneratedView` | UI-only generated-invoice consumption view with invoice card and sold-item summary. | `{ record: Object, invoice: Object, items: Array, outletName: Function, visitLabel: Function, formatDate: Function }` | `open-invoice(code)` | `FRONTENT/src/components/Operations/Outlets/OutletConsumptionInvoiceGeneratedView.vue` |
| `OutletConsumptionReadonlyView` | UI-only read-only fallback view for cancelled/other outlet consumption states. | `{ record: Object, items: Array, outletName: Function, visitLabel: Function, formatDate: Function }` | `None` | `FRONTENT/src/components/Operations/Outlets/OutletConsumptionReadonlyView.vue` |
| `OutletHubSummaryStats` | UI-only four-tile summary card row for the Outlet Hub (planned visits, pending restocks, open returns, total outstanding). | `{ plannedVisits: Array, pendingRestocks: Array, pendingReturns: Array, totalOutstanding: Number, formatMoney: Function }` | `None` | `FRONTENT/src/components/Operations/Outlets/OutletHub/OutletHubSummaryStats.vue` |
| `OutletHubSimpleListSection` | UI-only generic outlet-hub list section (title, subtitle, action button, AqlList) for planned visits, restocks, returns, and recent invoices. | `{ title: String, subtitle: String, icon: String, color: String, items: Array, content: Array, meta: Array, metaLayout: Array, emptyText: String, clickable: Boolean, actionLabel: String, selectedOutletCode: String }` | `action()`, `navigate(item)` | `FRONTENT/src/components/Operations/Outlets/OutletHub/OutletHubSimpleListSection.vue` |
| `OutletHubPaymentSection` | UI-only outlet-hub unpaid invoices + recent payments section with per-invoice collect button and aggregated Payment Collection action. | `{ selectedOutletCode: String, unpaidInvoices: Array, recentPayments: Array, canCreatePayment: Boolean, formatMoney: Function, formatDisplayDate: Function, remainingFor: Function }` | `collect(invoiceCode)`, `payment-collection()` | `FRONTENT/src/components/Operations/Outlets/OutletHub/OutletHubPaymentSection.vue` |
| `ResourceActionButton` | Unified button that automatically handles action-level permissions and styling. | `{ action: [String, Array, Object], targetResource: String, hideIfUnauthorized: Boolean, color: String, icon: String, label: String, loading: Boolean, disable: Boolean, push: Boolean, glossy: Boolean, flat: Boolean, round: Boolean, dense: Boolean, unelevated: Boolean, outline: Boolean, fab: Boolean }` | `click` | `FRONTENT/src/components/shared/ResourceActionButton.vue` |
| `DataAddFAB` | Reusable page-sticky floating action button for record creation with elevated shadow styling and permissions check. Supports default page routing or custom local action triggering. | `{ position: String, offset: Array, icon: String, color: String, tooltip: String, action: String, targetResource: String, customClick: Boolean }` | `click(event)` | `FRONTENT/src/components/shared/DataAddFAB.vue` |
| `ActionCommentDialog` | Unified modal dialog containing optional/mandatory comment area and a ResourceActionButton submission flow. | `{ modelValue: Boolean, title: String, label: String, commentRequired: Boolean, commentRows: Number, saving: Boolean, submitLabel: String, submitColor: String, submitIcon: String, action: [String, Array, Object], targetResource: String, disableSubmit: Boolean }` | `update:modelValue, confirm(comment), cancel` | `FRONTENT/src/components/shared/ActionCommentDialog.vue` |
| `AqlList` | Unified, stateless, Quasar-native q-list and q-item list renderer. Supports dynamic layouts, top/center alignments, index-matched custom content flows, automatic pastel avatar styling, dynamic color-coded left highlight borders, built-in row action buttons, secondary stats side sections (with label, caption, and chip types), and scoped slot overrides (item, content[idx], meta[idx], avatar, btn). | `{ items: Array, itemKey: [String, Function], loading: Boolean, emptyText: String, bordered: Boolean, separator: Boolean, dense: Boolean, align: String, label: [String, Function], caption: [String, Function], layout: Array, content: Array, btn: [String, Function], btnColor: [String, Function], meta: Array, metaLayout: Array, metaColor: [String, Function], metaLabel: [String, Function], metaCaption: [String, Function], chip: [String, Function], chipColor: [String, Function], clickable: Boolean }` | `click(item)` | `FRONTENT/src/components/shared/AqlList.vue` |
