# Frontend Component Registry

Purpose: quick discovery of reusable UI blocks under `FRONTENT/src/components/`.

Rule: update this file whenever a new reusable component is added or a listed component's API changes.

Design principle:
- Prefer tiny, single-responsibility reusable components.
- Avoid page-private one-off wrappers unless there is a clear reuse path.

| Component Name | Description | Props | Events | Path |
|---|---|---|---|---|
| `MasterListHeader` | Master list header with title/subtitle, sync status, and record counters. | `{ config: Object, filteredCount: Number, totalCount: Number, loading: Boolean, backgroundSyncing: Boolean }` | `reload()` | `FRONTENT/src/components/master/MasterListHeader.vue` |
| `MasterListReportBar` | Toolbar-level report actions rendered as a standalone section card; hidden when no toolbar reports exist. | `{ reports: Array, isGenerating: Boolean }` | `generate-report(report)` | `FRONTENT/src/components/master/MasterListReportBar.vue` |
| `MasterListToolbar` | Search input for master list filtering. | `{ searchTerm: String }` | `update:searchTerm(value)` | `FRONTENT/src/components/master/MasterListToolbar.vue` |
| `MasterListRecords` | Card-list section with loading/empty states, field-based title/subtitle resolution, and child count badges per record. | `{ items: Array, loading: Boolean, resolvedFields: Array, childCountMap: Object }` | `navigate-to-view(row)` | `FRONTENT/src/components/master/MasterListRecords.vue` |
| `MasterListRecordsRecord` | Individual master record card with click-to-open detail (no status badge). | `{ row: Object, resolvePrimaryText: Function, resolveSecondaryText: Function }` | `open-detail(row)` | `FRONTENT/src/components/master/_common/MasterListRecordsRecord.vue` |
| `MasterListRecordsLoading` | Loading spinner for the master list. | `{}` | `None` | `FRONTENT/src/components/master/_common/MasterListRecordsLoading.vue` |
| `MasterListRecordsEmpty` | Empty state for the master list. | `{}` | `None` | `FRONTENT/src/components/master/_common/MasterListRecordsEmpty.vue` |
| `MasterListViewSwitcher` | Filter view chip bar for switching between named list views (Active/Inactive, custom filters). Shows counts per view, outlined/filled toggle for active state. Hidden when no views configured. | `{ views: Array, activeViewName: String, counts: Object }` | `update:activeViewName(name)` | `FRONTENT/src/components/master/MasterListViewSwitcher.vue` |
| `abstract/Breadcrumb` | Pure, stateless breadcrumb renderer (Resource > Code > Action). No composable/component imports and no `<style>` block — `.breadcrumb-bar` / `.crumb*` live in `src/css/custom.scss` (ARCHITECTURE RULES §7). Driven entirely by props. | `{ resourceTitle: String, code: String, actionLabel: String, isActionView: Boolean }` | `click-root()`, `click-code()` | `FRONTENT/src/components/abstract/Breadcrumb.vue` |
| `app/Breadcrumb` | Dependable breadcrumb container for resource sub-route pages. Shared across masters, operations, and accounts scopes. Resolves defaults via `useRouteConfig`/`useResourceConfig`/`useResourceNav`/`humanizeSlug` and delegates rendering to `abstract/Breadcrumb.vue`. Self-sufficient by default, overrideable via props. | `{ scope?: String, resourceSlug?: String, resourceTitle?: String, code?: String, action?: String, actionLabel?: String }` | — | `FRONTENT/src/components/app/Breadcrumb.vue` |
| `ReloadButton` | Shared resource-aware refresh button. Resolves current, parent, child, and `*Code` column dependencies and refreshes them through the resource IO sync flow. | `{}` | `None` | `FRONTENT/src/components/shared/ReloadButton.vue` |
| `AppDate` | Reusable Quasar date input with a popup calendar, configured to read/write native HTML `YYYY-MM-DD` formatted values. | `{ modelValue: String }` | `update:modelValue(value)` | `FRONTENT/src/components/shared/AppDate.vue` |
| `AppDate` (app) | App-level date control wrapping `abstract/Date.vue`; forwards all `$attrs` + slots and defaults to today (`YYYY-MM-DD`) via an **immediate watcher** on `modelValue` — so it re-seeds not just on mount but every time the value goes empty, e.g. when a form Reset swaps in a blank pageState node. Used by `useFormFields`/`FormRecord` for all date fields (type `date`/`datetime` or `*Date` headers). | `{ modelValue: String }` | `update:modelValue(value)` | `FRONTENT/src/components/app/Date.vue` |
| `abstract/Date` | Pure, stateless date input: masked `q-input` (`####-##-##`, `YYYY-MM-DD`) with a calendar `q-popup-proxy` holding a `minimal` `q-date`, auto-hiding the popup on pick. One-click today selection comes from an explicit **Today** button in the popup footer (beside Close) — Quasar's `today-btn` prop renders inside `.q-date__header`, which `minimal` removes, so the prop alone is inert here (kept for callers that drop `minimal`). Both controls coerce `modelValue \|\| ''` so a null/undefined value never reaches QInput's mask handling. | `{ modelValue: String }` | `update:modelValue(value)` | `FRONTENT/src/components/abstract/Date.vue` |
| `OutletVisitActionDialog` | Reusable bottom-sheet dialog for complete/postpone/cancel actions on a planned outlet visit. Emits `{ action, fields }` so the caller can drive the matching composable function. | `{ modelValue: Boolean, visit: Object, outletLabel: String, saving: Boolean }` | `update:modelValue(value)`, `confirm({ action, fields })`, `cancel` | `FRONTENT/src/components/operation/Outlets/OutletVisitActionDialog.vue` |
| `SectionDividerLabel` | Centered text overline between two horizontal rules, used to title mid-page sections (e.g. PRIORITY, HISTORY, VISITS). | `{ label: String }` | — | `FRONTENT/src/components/shared/SectionDividerLabel.vue` |
| `ChildRecordsTable` | Inline editable table for child resource records within Add/Edit/View pages. | `{ childResource: Object, records: Array, headers: Array, readonly: Boolean }` | `add`, `remove(index)`, `update(index, field, value)` | `FRONTENT/src/components/master/ChildRecordsTable.vue` |
| `BulkUploadControlsCard` | Left-side bulk upload controls: resource select, header input, file upload, and plotting. | `{ selectedResourceName: String, resourceOptions: Array, headersDisplay: String, rawContent: String, csvFile: Object }` | `update:selectedResourceName(value)`, `update:headersDisplay(value)`, `update:rawContent(value)`, `update:csvFile(file)`, `resourceSelected(value)`, `fileUpload(file)`, `downloadTemplate()`, `plotTable()` | `FRONTENT/src/components/master/BulkUpload/BulkUploadControlsCard.vue` |
| `BulkUploadPreviewTable` | Bulk upload preview/edit table with per-row inline editing and upload actions. | `{ rows: Array, columns: Array, isUploading: Boolean }` | `clearAll()`, `uploadAll()`, `deleteRow(row)`, `cellEdited(row, field, value)` | `FRONTENT/src/components/master/BulkUpload/BulkUploadPreviewTable.vue` |
| `BulkUploadEmptyState` | Empty-state panel for bulk upload page before plotting rows. | `{}` | `None` | `FRONTENT/src/components/master/BulkUpload/BulkUploadEmptyState.vue` |
| `ResourceReports` | [LEGACY — direct-import path] Self-contained, context-aware report list and trigger container. Automatically detects scope, resolves the active record from the route code via `useDataStore`, renders buttons, and handles inputs. Retained unchanged for the custom views and pages that import it directly. **New work should use the Action-subsystem `ActionsResourceReports` instead** (see the `Action Subsystem` block below), which resolves its record from the injected `resourceRecord` and is overridable at all 10 `_ui/` tiers. | `{ record: Object, inline: Boolean, mode: String }` | — | `FRONTENT/src/components/Reports/ResourceReports.vue` |
| `ReportInputDialog` | Dynamic form dialog for collecting user inputs before PDF report generation. Renders text/number/date/boolean/select fields based on report config. Now a thin body over `AqlDialog` — it supplies only the field list; header, banner, footer, transitions and sizing come from the shell, and it carries no `<style>` block of its own. Fields render at Quasar's standard (non-`dense`) height for comfortable touch targets. Lives in `app/` because it composes a shared component and reads `useDataStore` to populate dynamic selects, which `abstract/` forbids. | `{ modelValue: Boolean, report: Object, formValues: Object, isGenerating: Boolean }` | `update:modelValue(value)`, `update:formValues(values)`, `confirm()`, `cancel()` | `FRONTENT/src/components/app/ReportInputDialog.vue` |
| `MasterViewHeader` | View page header with record code, primary text title, and status badge. | `{ config, record, resourceName, code, primaryText }` | — | `FRONTENT/src/components/master/MasterViewHeader.vue` |
| `MasterViewActionBar` | [DEPRECATED] Replaced by modular FAB components. | `{ config, record, code, resourceSlug, scope, additionalActions, permissions }` | — | `FRONTENT/src/components/_common/Action/ActionBar.vue` |
| `Downloads` | Reports FAB component displayed on bottom-left. | `{ record: Object, page: String }` | — | `FRONTENT/src/components/_common/sections/Action/Downloads.vue` |
| `DownloadsPageSticky` | Sticky positioning wrapper for Downloads. | `{ page: String, position: String, offset: Array }` | — | `FRONTENT/src/components/_common/sections/Action/Downloads/PageSticky.vue` |
| `DownloadsReportFab` | Main floating reports FAB menu for Downloads. | `{ page: String, isGenerating: Boolean, reportsCount: Number }` | — | `FRONTENT/src/components/_common/sections/Action/Downloads/ReportFab.vue` |
| `DownloadsReportItem` | Report action sub-item button inside the FAB menu. | `{ page: String, report: Object }` | `click()` | `FRONTENT/src/components/_common/sections/Action/Downloads/ReportItem.vue` |
| `CrudActions` | CRUD floating buttons/FAB displayed on bottom-right (Add/Edit triggers). | `{ page: String }` | `edit()`, `add()` | `FRONTENT/src/components/_common/sections/Action/CrudActions.vue` |
| `CrudActionsPageSticky` | Sticky positioning wrapper for CrudActions. | `{ page: String, position: String, offset: Array }` | — | `FRONTENT/src/components/_common/sections/Action/CrudActions/PageSticky.vue` |
| `CrudActionsFabBtn` | Main expandable menu trigger button for CrudActions. | `{ page: String, permissions: Object }` | — | `FRONTENT/src/components/_common/sections/Action/CrudActions/FabBtn.vue` |
| `CrudActionsFabBtnAdd` | Floating Add action button. | `{ page: String, permissions: Object }` | `add()` | `FRONTENT/src/components/_common/sections/Action/CrudActions/FabBtnAdd.vue` |
| `CrudActionsFabBtnEdit` | Floating Edit action button. | `{ page: String, permissions: Object }` | `edit()` | `FRONTENT/src/components/_common/sections/Action/CrudActions/FabBtnEdit.vue` |
| `CrudActionsFabBtnView` | Floating View action button. | `{ page: String, permissions: Object }` | `view()` | `FRONTENT/src/components/_common/sections/Action/CrudActions/FabBtnView.vue` |
| `CrudActionsFabItemAdd` | Add sub-action inside the main FAB menu. | `{ page: String, permissions: Object }` | `add()` | `FRONTENT/src/components/_common/sections/Action/CrudActions/FabItemAdd.vue` |
| `CrudActionsFabItemEdit` | Edit sub-action inside the main FAB menu. | `{ page: String, permissions: Object }` | `edit()` | `FRONTENT/src/components/_common/sections/Action/CrudActions/FabItemEdit.vue` |
| `CrudActionsFabItemView` | View sub-action inside the main FAB menu. | `{ page: String, permissions: Object }` | `view()` | `FRONTENT/src/components/_common/sections/Action/CrudActions/FabItemView.vue` |
| `AdditionalActionSingle` | Single workflow action floating button. | `{ action: Object, page: String }` | `action-clicked(action)` | `FRONTENT/src/components/_common/sections/Action/AdditionalActionSingle.vue` |
| `AdditionalActionMultiple` | Multiple workflow actions expandable FAB. | `{ actions: Array, page: String }` | `action-clicked(action)` | `FRONTENT/src/components/_common/sections/Action/AdditionalActionMultiple.vue` |
| `AdditionalActionsPageSticky` | Sticky positioning wrapper for AdditionalActions. | `{ page: String, position: String, offset: Array }` | — | `FRONTENT/src/components/_common/sections/Action/AdditionalActions/PageSticky.vue` |
| `AdditionalActionsFabSingleBtn` | Single action trigger button. | `{ page: String, action: Object }` | `click()` | `FRONTENT/src/components/_common/sections/Action/AdditionalActions/FabSingleBtn.vue` |
| `AdditionalActionsFabBtn` | Main floating reports FAB menu for AdditionalActions. | `{ page: String, actions: Array }` | — | `FRONTENT/src/components/_common/sections/Action/AdditionalActions/FabBtn.vue` |
| `AdditionalActionsFabItem` | Individual workflow action sub-item inside the menu. | `{ page: String, action: Object }` | `click()` | `FRONTENT/src/components/_common/sections/Action/AdditionalActions/FabItem.vue` |
| `FormActions` | [DEPRECATED] Replaced by the Action subsystem — see the `Action Subsystem` block below. | `{ page: String, submitLabel: String, saving: Boolean, disabled: Boolean }` | `cancel()`, `submit()` | `FRONTENT/src/components/_common/sections/Action/FormActions.vue` |
| `FormActionsStickyBar` | Sticky positioning panel wrapper for form actions. | `{ page: String }` | — | `FRONTENT/src/components/_common/sections/Action/FormActions/StickyBar.vue` |
| `FormActionsFormCancel` | Cancel button inside the form actions bar. | `{ page: String, label: String, icon: String, color: String, flat: Boolean }` | `cancel()` | `FRONTENT/src/components/_common/sections/Action/FormActions/FormCancel.vue` |
| `FormActionsFormSubmit` | Submit button inside the form actions bar. | `{ page: String, label: String, saving: Boolean, disabled: Boolean, color: String, unelevated: Boolean, icon: String }` | `submit()` | `FRONTENT/src/components/_common/sections/Action/FormActions/FormSubmit.vue` |
| `ActionDialog` | Dynamic in-place mutate action input modal. | `{ modelValue: Boolean, actionConfig: Object, record: Object }` | `update:modelValue(value)` | `FRONTENT/src/components/_common/sections/Action/ActionDialog.vue` |
| `MasterViewDetails` | Key-value detail grid for record fields on the View page. | `{ record, resolvedFields, resourceHeaders }` | — | `FRONTENT/src/components/master/MasterViewDetails.vue` |
| `MasterViewAudit` | Created/Updated audit timestamps on the View page. | `{ record }` | — | `FRONTENT/src/components/master/MasterViewAudit.vue` |
| `MasterViewChildren` | Child resource record tables on the View page. | `{ childResources, record, code }` | — | `FRONTENT/src/components/master/MasterViewChildren.vue` |
| `MasterViewChild` | Single child resource section on the View page. | `{ childResource: Object, childRecords: Array, additionalActions: Array }` | `view-child(childResource, code)` | `FRONTENT/src/components/master/_common/MasterViewChild.vue` |
| `MasterViewLoading` | Loading spinner for the master view page. | `{}` | `None` | `FRONTENT/src/components/master/_common/MasterViewLoading.vue` |
| `MasterViewEmpty` | Empty state for the master view page. | `{}` | `back` | `FRONTENT/src/components/master/_common/MasterViewEmpty.vue` |
| `WarehouseStockRows` | UI-only warehouse stock renderer with desktop table and mobile list views. | `{ rows: Array }` | `None` | `FRONTENT/src/components/master/Warehouses/WarehouseStockRows.vue` |
| `MasterAddHeader` | Add page header with "Create {ResourceName}" title. | `{ config, resourceName }` | — | `FRONTENT/src/components/master/MasterAddHeader.vue` |
| `MasterAddForm` | Add page form fields (status select + text inputs). | `{ resolvedFields, parentForm, resourceHeaders }` | `update:field(header, value)` | `FRONTENT/src/components/master/MasterAddForm.vue` |
| `MasterAddChildren` | Add page child records table with inline editing. | `{ childResources, childRecords }` | `add-child(name)`, `remove-child(name, index)`, `update-child-field(name, index, field, value)` | `FRONTENT/src/components/master/MasterAddChildren.vue` |
| `MasterAddActions` | Add page cancel/submit action buttons. | `{ saving, submitLabel }` | `cancel()`, `submit()` | `FRONTENT/src/components/master/MasterAddActions.vue` |
| `MasterEditHeader` | Edit page header with "Edit {ResourceName} — {Code}" title. | `{ config, resourceName, code }` | — | `FRONTENT/src/components/master/MasterEditHeader.vue` |
| `MasterEditForm` | Edit page form with read-only code field + editable fields. | `{ resolvedFields, parentForm, resourceHeaders, code }` | `update:field(header, value)` | `FRONTENT/src/components/master/MasterEditForm.vue` |
| `MasterEditChildren` | Edit page child records table with inline editing. | `{ childResources, childRecords }` | `add-child(name)`, `remove-child(name, index)`, `update-child-field(name, index, field, value)` | `FRONTENT/src/components/master/MasterEditChildren.vue` |
| `MasterEditActions` | Edit page cancel/submit action buttons. | `{ saving, submitLabel }` | `cancel()`, `submit()` | `FRONTENT/src/components/master/MasterEditActions.vue` |
| `MasterActionHeader` | Action page header with action icon, label, and record code. | `{ config, record, code, actionConfig, primaryText }` | — | `FRONTENT/src/components/master/MasterActionHeader.vue` |
| `MasterActionForm` | Action page form with outcome selector and dynamic fields. | `{ actionConfig, actionFields, actionForm }` | `update:field(key, value)`, `update:outcome(value)` | `FRONTENT/src/components/master/MasterActionForm.vue` |
| `MasterActionActions` | Action page submit/cancel buttons with configurable label/icon/color. | `{ actionConfig, saving, submitDisabled }` | `cancel()`, `submit()` | `FRONTENT/src/components/master/MasterActionActions.vue` |
| `StockEntryGrid` | Editable stock register for `/operation/stock-movements/direct-entry`. Loads existing stock for a warehouse, allows inline editing and new rows, handles save deltas. | `{ warehouseCode: String }` | — | `FRONTENT/src/components/operation/StockMovements/StockEntryGrid.vue` |
| `GrnStockEntryGrid` | Mobile-friendly GRN allocation grid for `/operation/stock-movements/grn-entry`. Displays accepted GRN items, storage splits, current stock, and allocation balance. | `{ items: Array, allocationGroups: Object, summary: Object, storageOptions: Array, currentStockFor: Function }` | `update-allocation(rowId, patch)`, `add-allocation(itemCode)`, `remove-allocation(rowId)` | `FRONTENT/src/components/operation/StockMovements/GrnStockEntryGrid.vue` |
| `MenuTreeNode` | Recursive sidebar menu node renderer. Renders a `q-expansion-item` (group) or `q-item` (leaf) based on `node.type`. Used by `MainLayout.vue` to build an N-level sidebar tree from `group` path arrays. | `{ node: Object }` (node has `type: 'group'|'leaf'`, `key`, `label/navLabel`, `icon/navIcon`, `children?`, `routePath?`) | — | `FRONTENT/src/components/MenuTreeNode.vue` |
| `PurchaseRequisitionAddItemDialog` | Bottom-sheet add-item dialog used by PR create/review flows for SKU selection and quantity/rate entry. | `{ modelValue, newItem, skuOptions, formatCurrency }` | `update:modelValue(value)`, `filter-skus(...)`, `confirm-add-item()` | `FRONTENT/src/components/operation/PurchaseRequisitions/PurchaseRequisitionAddItemDialog.vue` |
| `RfqSupplierFlowPage` | Shared RFQ supplier dispatch page body used by the assign-supplier and mark-as-sent record pages. Handles supplier selection, assignment, dispatch marking, and the supporting RFQ summary panels. | `{ mode: String }` | `None` | `FRONTENT/src/components/operation/Rfqs/RfqSupplierFlowPage.vue` |
| `POReceivingSummaryCards` | UI-only summary cards for PO Receiving totals. | `{ summary: Object }` | `None` | `FRONTENT/src/components/operation/PoReceivings/POReceivingSummaryCards.vue` |
| `POReceivingItemGrid` | UI-only editable/read-only PO Receiving item grid with scrollable desktop table, mobile list layout, and derived quantity display columns. | `{ items: Array, editable: Boolean }` | `update:item(index, patch)` | `FRONTENT/src/components/operation/PoReceivings/POReceivingItemGrid.vue` |
| `POReceivingBulkToolbar` | UI-only bulk quantity actions for PO Receiving draft lines. | `{}` | `receive-all()`, `clear()` | `FRONTENT/src/components/operation/PoReceivings/POReceivingBulkToolbar.vue` |
| `POReceivingReportLinks` | Disabled report placeholder list for future damage/reject/short/excess reports. | `{ reports: Array }` | `None` | `FRONTENT/src/components/operation/PoReceivings/POReceivingReportLinks.vue` |
| `GoodsReceiptItemsTable` | UI-only accepted-quantity item table for Goods Receipt views. | `{ items: Array }` | `None` | `FRONTENT/src/components/operation/GoodsReceipts/GoodsReceiptItemsTable.vue` |
| `GenericHeaderPanel` | Shared header composing HeaderPanel with optional back button, status chip, and reload button. | `{ label: String, caption: String, icon: String, iconColor: String, chip: String, chipColor: String, chipTextColor: String, back: [String,Boolean], backIcon: String, reload: [String,Boolean], leftIconColor: String }` | `click` | `FRONTENT/src/components/shared/GenericHeaderPanel.vue` |
| `HeaderPanel` | Summary header card for resource/operation pages with title, subtitle, optional icon, and side slot. | `{ title: String, subtitle: String, icon: String, iconSize: String, iconColor: String }` | `None` | `FRONTENT/src/components/shared/HeaderPanel.vue` |
| `HeaderPanelWithReload` | Simple header row combining HeaderPanel title/subtitle with an inline ReloadButton. | `{ title: String, subtitle: String }` | `None` | `FRONTENT/src/components/shared/HeaderPanelWithReload.vue` |
| `OutletItemGrid` | UI-only editable/read-only outlet SKU quantity grid used by restock, delivery, and consumption rows. Supports optional storage column and configurable quantity columns. | `{ items: Array, editable: Boolean, showStorage: Boolean, quantityColumns: Array }` | `update:item(index, patch)`, `remove:item(index)` | `FRONTENT/src/components/operation/Outlets/OutletItemGrid.vue` |
| `OutletProgressChip` | UI-only chip that maps outlet workflow status/progress values to shared labels/colors. | `{ progress: String }` | `None` | `FRONTENT/src/components/operation/Outlets/OutletProgressChip.vue` |
| `RestockApprovalView` | UI-only outlet restock approval review with grouped availability sections, workflow comments, recommendation actions, approval, send-back, and rejection. | `{ restock: Object, approvalGroups: Array, hasAllocatedRows: Boolean, outletName: String, approveLoading: Boolean, sendBackLoading: Boolean, rejectLoading: Boolean, formatWorkflowCommentHtml: Function }` | `applyRecommendation(rowKey)`, `updateAllocationLine(payload)`, `addLine(rowKey)`, `removeLine(payload)`, `resetAllocation(rowKey)`, `approve(comment)`, `sendBack(comment)`, `reject(comment)` | `FRONTENT/src/components/operation/Outlets/RestockApprovalView.vue` |
| `RestockCard` | Outlet restock card with outlet label, date/user line, progress icon, urgency border, and item progress summary. | `{ restock: Object, outletLabel?: String, itemSummary?: String }` | `None` | `FRONTENT/src/components/operation/Outlets/RestockCard.vue` |
| `RestockDraftView` | Outlet restock edit panel for draft/resubmission flow with draft rows, quantities, and workflow comment actions. | `{ restock: Object, rows: Array, skuOptions: Array, outletName: String, saving: Boolean, formatWorkflowCommentHtml: Function, addRow: Function, updateRow: Function, removeRow: Function }` | `save-draft`, `submit` | `FRONTENT/src/components/operation/Outlets/RestockDraftView.vue` |
| `RestockReadonlyView` | Outlet restock read-only panel for approved/delivered history with ORSI progress, warehouse, storage, and workflow summary. | `{ restock: Object, rows: Array, skuOptions: Array, outletName: String, formatWorkflowCommentHtml: Function }` | `None` | `FRONTENT/src/components/operation/Outlets/RestockReadonlyView.vue` |
| `OrsiAllocationRow` | UI-only ORSI allocation request renderer with prepared recommendation/allocation lines, pending remainder display, and line-level intent events. | `{ rowView: Object }` | `applyRecommendation(rowKey)`, `updateAllocationLine(payload)`, `addLine(rowKey)`, `removeLine(payload)`, `resetAllocation(rowKey)` | `FRONTENT/src/components/operation/Outlets/OrsiAllocationRow.vue` |
| `OutletDeliveryItemRow` | UI-only ODI row with SKU/storage quantity, progress chip, and item delivery action. | `{ row: Object, canDeliver: Boolean }` | `deliver(row)` | `FRONTENT/src/components/operation/Outlets/OutletDeliveryItemRow.vue` |
| `OutletDeliverySummaryPanel` | Header summary for an OD record with outlet count and delivered/total item counts. | `{ delivery: Object, summary: Object }` | `None` | `FRONTENT/src/components/operation/Outlets/OutletDeliverySummaryPanel.vue` |
| `RestockSummaryBar` | Outlet restock summary card row with progress counts and role-aware filtering. | `{ counts: Object, permissions: Object }` | `filter(key)` | `FRONTENT/src/components/operation/Outlets/RestockSummaryBar.vue` |
| `VisitSummaryBar` | Outlet visit summary chip row for priority counts with scroll-to section behavior. | `{ stats: Object }` | `scrollTo(sectionKey)` | `FRONTENT/src/components/operation/Outlets/VisitSummaryBar.vue` |
| `OutletStockRows` | UI-only outlet stock list with SKU/outlet labels, storage display, quantity badge, empty state, and row selection. | `{ rows: Array, skuLabel: Function, outletLabel: Function }` | `select(row)` | `FRONTENT/src/components/operation/Outlets/OutletStockRows.vue` |
| `OutletMovementTimeline` | UI-only timeline for outlet movement ledger rows, showing reference type, quantity, date, SKU, storage, and reference code. | `{ movements: Array }` | `None` | `FRONTENT/src/components/operation/Outlets/OutletMovementTimeline.vue` |
| `OutletConsumptionPendingInvoiceView` | UI-only pending-invoice consumption view with human-readable context and generate-invoice command. | `{ record: Object, items: Array, acting: Boolean, outletName: Function, visitLabel: Function, formatDate: Function }` | `generate-invoice()` | `FRONTENT/src/components/operation/Outlets/OutletConsumptionPendingInvoiceView.vue` |
| `OutletConsumptionInvoiceGeneratedView` | UI-only generated-invoice consumption view with invoice card and sold-item summary. | `{ record: Object, invoice: Object, items: Array, outletName: Function, visitLabel: Function, formatDate: Function }` | `open-invoice(code)` | `FRONTENT/src/components/operation/Outlets/OutletConsumptionInvoiceGeneratedView.vue` |
| `OutletConsumptionReadonlyView` | UI-only read-only fallback view for cancelled/other outlet consumption states. | `{ record: Object, items: Array, outletName: Function, visitLabel: Function, formatDate: Function }` | `None` | `FRONTENT/src/components/operation/Outlets/OutletConsumptionReadonlyView.vue` |
| `OutletHubSummaryStats` | UI-only four-tile summary card row for the Outlet Hub (planned visits, pending restocks, open returns, total outstanding). | `{ plannedVisits: Array, pendingRestocks: Array, pendingReturns: Array, totalOutstanding: Number, formatMoney: Function }` | `None` | `FRONTENT/src/components/operation/Outlets/OutletHub/OutletHubSummaryStats.vue` |
| `OutletHubSimpleListSection` | UI-only generic outlet-hub list section (title, subtitle, action button, AqlList) for planned visits, restocks, returns, and recent invoices. | `{ title: String, subtitle: String, icon: String, color: String, items: Array, content: Array, meta: Array, metaLayout: Array, emptyText: String, clickable: Boolean, actionLabel: String, selectedOutletCode: String }` | `action()`, `navigate(item)` | `FRONTENT/src/components/operation/Outlets/OutletHub/OutletHubSimpleListSection.vue` |
| `OutletHubPaymentSection` | UI-only outlet-hub unpaid invoices + recent payments section with per-invoice collect button and aggregated Payment Collection action. | `{ selectedOutletCode: String, unpaidInvoices: Array, recentPayments: Array, canCreatePayment: Boolean, formatMoney: Function, formatDisplayDate: Function, remainingFor: Function }` | `collect(invoiceCode)`, `payment-collection()` | `FRONTENT/src/components/operation/Outlets/OutletHub/OutletHubPaymentSection.vue` |
| `ResourceActionButton` | Unified button that automatically handles action-level permissions and styling. | `{ action: [String, Array, Object], targetResource: String, hideIfUnauthorized: Boolean, color: String, icon: String, label: String, loading: Boolean, disable: Boolean, push: Boolean, glossy: Boolean, flat: Boolean, round: Boolean, dense: Boolean, unelevated: Boolean, outline: Boolean, fab: Boolean }` | `click` | `FRONTENT/src/components/shared/ResourceActionButton.vue` |
| `DataAddFAB` | Reusable page-sticky floating action button for record creation with elevated shadow styling and permissions check. Supports default page routing or custom local action triggering. | `{ position: String, offset: Array, icon: String, color: String, tooltip: String, action: String, targetResource: String, customClick: Boolean }` | `click(event)` | `FRONTENT/src/components/shared/DataAddFAB.vue` |
| `ActionCommentDialog` | Unified modal dialog containing optional/mandatory comment area and a ResourceActionButton submission flow. Built on `AqlDialog`, overriding only `#actions` so the confirm button stays a `ResourceActionButton` (it carries the per-action permission gate the generic footer cannot evaluate). New `variant` prop forwards the header/banner tint, so a destructive confirmation reads as negative with no template change at the call site. | `{ modelValue: Boolean, title: String, label: String, commentRequired: Boolean, commentRows: Number, saving: Boolean, submitLabel: String, submitColor: String, submitIcon: String, variant: String, action: [String, Array, Object], targetResource: String, disableSubmit: Boolean }` | `update:modelValue, confirm(comment), cancel` | `FRONTENT/src/components/shared/ActionCommentDialog.vue` |
| `AqlDialog` | **The shared premium modal shell — compose this instead of hand-rolling a `q-dialog > q-card` stack.** Stateless (imports no composable/store/service). Rounded 20px card with `shadow-10` and a scale/fade transition; a variant-tinted header band (icon avatar + title + subtitle + close), a scrollable body with an optional one-line `q-banner`, and a `bg-grey-1` footer with `push glossy` Confirm / flat Cancel. Three override levels: props only, the default slot for body content, or the `#header` / `#actions` slots to replace the chrome — `#actions` is scoped with `{ loading, disabled }`. No `<style>` block; all rules are `.aql-dialog-*` in `custom.scss` (ARCHITECTURE RULES §7). | `{ modelValue: Boolean, title/subtitle/icon: String, variant: String ('primary'\|'info'\|'warning'\|'negative'\|'positive'), showClose: Boolean, message/messageIcon: String, hideActions: Boolean, confirmLabel/confirmIcon/confirmColor: String, cancelLabel/cancelIcon: String, loading/disableConfirm: Boolean, persistent/maximized: Boolean, width/maxWidth/minWidth: String }` | `update:modelValue(value)`, `confirm()`, `cancel()` | `FRONTENT/src/components/shared/AqlDialog.vue` |
| `AqlGroupedList` | Stateless flat-item grouped card/list renderer. Groups internally by `groupKey`, renders a required header label, optional header icon, default badge or header-right slot, and customizable child item label, caption, avatar/icon, badge/chip, and scoped item slots. | `{ items: Array, rootClass: [String, Array, Object], groupKey: [String, Function], groupLabel: [String, Function], itemKey: [String, Function], loading: Boolean, emptyText: String, headerLabel: [String, Function], headerCaption: [String, Function], headerIcon: [String, Function], headerBadge: [String, Number, Function], itemLabel: [String, Function], itemCaption: [String, Function], itemIcon: [String, Function], itemAvatar: [String, Function], itemAvatarLabel: [String, Function], itemBadge: [String, Number, Function], itemChip: [String, Number, Function] }` | `None` | `FRONTENT/src/components/shared/AqlGroupedList.vue` |
| `AqlList` | Unified, stateless, Quasar-native q-list and q-item list renderer. Supports dynamic layouts, top/center alignments, index-matched custom content flows, automatic pastel avatar styling, dynamic color-coded left highlight borders, built-in row action buttons, secondary stats side sections (with label, caption, and chip types), and scoped slot overrides (item, content[idx], meta[idx], avatar, btn). | `{ items: Array, itemKey: [String, Function], loading: Boolean, emptyText: String, bordered: Boolean, separator: Boolean, dense: Boolean, align: String, label: [String, Function], caption: [String, Function], layout: Array, content: Array, btn: [String, Function], btnColor: [String, Function], meta: Array, metaLayout: Array, metaColor: [String, Function], metaLabel: [String, Function], metaCaption: [String, Function], chip: [String, Function], chipColor: [String, Function], clickable: Boolean }` | `click(item)` | `FRONTENT/src/components/shared/AqlList.vue` |
| `StepProgressIndicator` | Reusable step progress indicator for multi-step wizards/pages. | `{ modelValue: Number\|String, steps: Array, clickable?: Boolean, activeColor?: String, inactiveColor?: String, activeTextColor?: String, inactiveTextColor?: String }` | `update:modelValue(value)`, `click-step(name)` | `FRONTENT/src/components/shared/StepProgressIndicator.vue` |
| `AqlFilePreviewCard` | UI card for rendering uploaded files with thumbnails/icons, preview lightbox, and download capabilities. | `{ uuid: String, resourceName: String, columnName: String }` | — | `FRONTENT/src/components/shared/AqlFilePreviewCard.vue` |
| `AqlFileUpload` | Dynamic upload widget for staging, uploading to temp, validating file types and sizes, and confirmation/deletion control. | `{ modelValue: String, label: String, required: Boolean, accept: String, maxSize: Number, resourceName: String, columnName: String }` | `update:modelValue(value)` | `FRONTENT/src/components/shared/AqlFileUpload.vue` |
| `AqlStatusToggle` | Outlined toggle field aligning with q-input, showing label on the left and q-toggle on the right with a state label. | `{ modelValue: [Boolean, String], label: String, trueValue: [Boolean, String], falseValue: [Boolean, String], trueLabel: String, falseLabel: String, color: String }` | `update:modelValue(value)` | `FRONTENT/src/components/shared/AqlStatusToggle.vue` |
| `Content` | Dynamic content placeholder rendered inside `AqlContentWrapper`. Resolves the matching `components/contents/*` component (plus tenant overrides/modifiers) through `useContentResolver`, showing a spinner while resolving and a "Content Not Defined" card when nothing matches. Content counterpart of `Section.vue`. | `{ content: String }` + all orchestrator `pageProps` via `$attrs` | — | `FRONTENT/src/components/Content.vue` |
| `ContentsList` | Generic `List` content component (registered as page contract `contents: ['List']`). Consumes the injected `resourceRecord` (`filteredRecords` → `records`, `activeView`) and `resourceConfig`, derives resource- and list-view-aware label/caption/meta/highlight resolvers via `useListStrategy`, and renders `AppList`. Click navigates to `view` by default, to `record-page` (`{ code, pageSlug: target }`) when `target` is set, or to a caller-supplied `onItemClick`. | Full `abstract/List.vue` surface, all defaulting to `undefined` (`items`, `label`, `caption`, `layout`, `content`, `meta`, `metaLayout`, `chip*`, `badge*`, `btn*`, `highlight*`, `icon`, `avatar*`, `clickable`, …) plus `onItemClick: Function`, `target: String` | — | `FRONTENT/src/components/contents/List.vue` |
| `AppList` | App-level list wrapper over `abstract/List.vue`. Forwards every attribute, listener, and slot through unchanged (`inheritAttrs: false`) so app/content layers can compose the list without redeclaring its API. | All `abstract/List.vue` props via `$attrs` | `click(item)` (passthrough) | `FRONTENT/src/components/app/AppList.vue` |
| `ContentsView` | Main view-page content orchestrator. Renders Details (`ViewRecord`), Children, Parents, and Audit sections in configurable order with staggered entrance. Injects `resourceConfig`/`resourceRecord`/`pageState`. | `{ order: Array, hide: Array, detailsConfig: Object }` | — | `FRONTENT/src/components/contents/View.vue` |
| `ContentsViewRecord` | Key-value field grid for a single record. Renders Code + nav link (optional), filtered fields, level-2 nested object display, file previews, and column-level custom UI overrides via `useViewColumnResolver`. Accepts `resolvedFields` or falls back to raw key derivation. | `{ record: Object, resolvedFields: Array, resourceName: String, resourceSlug: String, scope: String, uiName: String, detailsConfig: Object, showCodeLink: Boolean }` | — | `FRONTENT/src/components/contents/ViewRecord.vue` |
| `ContentsViewRecordWithAudit` | Thin composition of `ViewRecord` + `ViewAudit`. | Same surface as `ViewRecord` (forwarded) | — | `FRONTENT/src/components/contents/ViewRecordWithAudit.vue` |
| `ContentsViewParent` | Resolves parent records from `record._Parents`, looks up `ViewParent{ResourceName}` overrides via custom UI, falls back to `ViewRecord` per parent resource. Injects `resourceConfig`/`resourceRecord`. | — (inject-driven) | — | `FRONTENT/src/components/contents/ViewParent.vue` |
| `ContentsViewChildren` | Children orchestrator. Per child resource: resolves `ViewChild{ResourceName}` custom UI override; renders a `SectionDividerLabel` per group, then ≤5 display cols → one `ViewChildCompact` grid, >5 cols → one card per record delegated to `ViewRecord` (full column/JS-modifier/file-preview support) or a per-record override. | — (inject-driven) | `view-child(childResource, code)` (handled internally) | `FRONTENT/src/components/contents/ViewChildren.vue` |
| `ContentsViewChildCompact` | Compact child view for ≤5 displayable columns. All records in one card as a `q-markup-table` (no `QTable`). Row click navigates. | `{ childResource: Object, childRecords: Array, fields: Array }` | `view-child(childResource, code)` | `FRONTENT/src/components/contents/ViewChildCompact.vue` |
| `ContentsViewAudit` | Audit card showing `CreatedAt`/`UpdatedAt` (not By fields). Hidden when neither is present. Injects `resourceRecord`. Audit is ONLY displayed here. | — (inject-driven) | — | `FRONTENT/src/components/contents/ViewAudit.vue` |
| _Full spec_ | `Create`/`Update`/`FormRecord`/`FormChild` below are summarized; the canonical reference (complete prop tables, precedence chain, override hierarchies, function-resolution rules, Update hydration, child soft-deletion) is [AQL_CREATE_AND_UPDATE_CONTENT_SYSTEM.md](file:///f:/LITTLE%20LEAP/AQL/Documents/AQL_CREATE_AND_UPDATE_CONTENT_SYSTEM.md). | — | — | — |
| `ContentsCreate` | `Create` content (page contract `contents: ['Create']`). Renders the primary-resource `FormRecord` plus one `FormChild` (or a resolved per-child `FormChild<ChildName>` override) per eligible, non-hidden child resource (`ParentResource` === active resource; master scope surfaces master children only; `hideChild`/`hideChildren` suppress specific children by name/slug, plus a dynamic per-child `hide<ResourceName>: true` prop/`$attrs` flag checked against name/slug/PascalCase name). Initializes/resets the primary pageState node on resource change, routes field edits through `pageState.setField`/`setControlField` (custom fields). `Status`/workflow-stamp hiding is handled inside `FormRecord`; `showStatus`/`statusDefault`/`workflowFields` are forwarded. Zero-hardcoding: every default title/class/behaviour is a prop, and `$attrs` are forwarded to the primary `FormRecord` + every `FormChild`. Parent-relation forms are never rendered; submit belongs to PageAction sections. | `{ withChildren: Boolean (true), hideChild/hideChildren: [String,Array] (null), childMode: String ('inline'), listPosition: String ('top'), closeOnAdd: Boolean (true), hideFields: Array, showFields: Array, showCode: Boolean (false), hideParentLink: Boolean (true), childHideFields: Array, workflowFields: [String,Boolean] ('hide'), showStatus: [Boolean,String] (false), statusDefault: String ('Active'), fields: Array, defaultValues: [Object,Function] ({}), fieldProps: [Object,Function] ({}), columns: Number (1), title: String, recordTitleFallback: String ('Details'), sectionTitles: Object, formRecordProps: Object, formChildProps: Object, sectionClass: [String,Array,Object] ('cf-section'), sectionStagger: Number (60) }` | — | `FRONTENT/src/components/contents/Create.vue` |
| `ContentsFormRecord` | Atomic bottom-layer form renderer (form counterpart of `ViewRecord.vue`; shared by Create and the upcoming Update). Resolves controls via `useFormFields` (text/number/textarea inputs, `app/Date.vue` for date/`*Date` fields, selects incl. linkRef cross-ref options AND `AppOptions` `<Resource><Column>` groups, toggles, `AqlFileUpload`, `AqlStatusToggle`); an explicit `fields` order may reference non-schema headers (`custom: true`), which fall back to a bare `QInput` and must be routed to `pageState.setControlField` (never `node.record`). Field visibility follows a strict **`showFields` > `hideFields` > `workflowFields`** chain (workflow stamps matching `/(.+?)(By\|At\|Comment)$/` hidden first, then `Status` unless `showStatus`, then `Code` unless `showCode`, then `hideFields`, then `showFields` forcibly un-hides). `Code` is fully editable when rendered — `useFormFields` carries no `disable: true` for it. Control props merge `$attrs` < `useFormFields` base < `fieldProps[header]` < `FormField<Header>.js` modifier. Self-resolves a whole-form `FormRecord.(vue\|js)` override and, per field, a `FormField<Header>.(vue\|js)` override — **`_ui/*` ONLY, no `components/shared` fallback** — via `scope`/`resourceSlug`/`uiName`. `defaultValues` may be an Object, a `(record, ctx) => Object` function, or an object of `(record, ctx) => value` functions; layered OVER backend `APP.Resources.DefaultValues` (resolved via `useResourceConfig(resource).defaultValues` — never a direct `useAuthStore` import, per `ARCHITECTURE RULES.md` §5 — works identically for primary and child forms) and UNDER the `Status` fallback; seeded via emitted `update:field` only when the header is `undefined`. Zero-hardcoding: all classes/labels/stagger are props; `$attrs` forward to the override, card wrapper, and every field control. | `{ resource: String, record: Object, title: String, hideFields: Array, showFields: Array, fields: Array, workflowFields: [String,Boolean] ('hide'), showStatus: [Boolean,String] (false), statusDefault: String ('Active'), showCode: Boolean (false), defaultValues: [Object,Function] ({}), fieldProps: [Object,Function] ({}), columns: Number (1), scope/resourceSlug/uiName: String, card: Boolean (true), cardClass/cardFlat/cardBordered, gridClass, fieldClass, colClass ('col-12'), colClassMulti ('col-12 col-sm-6'), emptyText, emptyClass, fieldStagger: Number (40) }` | `update:field(header, value, { custom: Boolean })` | `FRONTENT/src/components/contents/FormRecord.vue` |
| `ContentsFormChild` | Child-record entry container for Create/Update contents; every record input is rendered by `FormRecord` (passed the child resource's own `scope`/`resourceSlug`/`uiName` for its override resolution), every mutation lands in the pageState child bucket (`addChild`/`updateChild`/`removeChild`). Every new entry (inline/popup draft reset, multi Add Row) is pre-filled via `createChildDefaultRecord()` — `childResource.defaultValues` (backend `APP.Resources.DefaultValues`) < `defaultValues` prop < `Status` fallback — not just the first. `maxRecords` (`0` = unlimited) caps added record entry count; once reached, every Add affordance (inline submit, popup trigger + dialog submit, multi Add Row) is disabled for new entries while editing existing rows remains unaffected. Added-record list renders via `AppList` (function `itemKey`/`label`/`caption`, `#btn` slot with Edit+Delete, compact `#empty`), inheriting FLIP `aql-list-item` transitions and mobile-first `q-item` layout for free. Three modes: `inline` (default — permanent entry form, list positioned via `listPosition`), `popup` (list + Add button opening a `q-dialog` form, `closeOnAdd` behavior, edit reopens prefilled), `multi`/`multiple` (one always-editable `FormRecord` card per row + Add/remove row buttons, no separate list). Zero-hardcoding: every label, icon, colour, card class, dialog size, and caption rule is a prop; `$attrs` forward to `AppList` + every inner `FormRecord`. | `{ childResource: Object, parentResource: String, childMode: String ('inline'), listPosition: String ('top'), closeOnAdd: Boolean (true), maxRecords: Number (0), hideFields: Array, showFields: Array, showCode: Boolean (false), columns: Number (1), title: String, fields/workflowFields/showStatus/statusDefault/defaultValues/fieldProps (forwarded to FormRecord), formRecordProps: Object, listProps: Object, listDense/listSeparator/listBordered, listClassTop/listClassBottom, itemLabel/itemCaption: [String,Function], captionParts: Number (3), captionSeparator: String (' • '), newItemLabel, emptyText, emptyClass, cardClass/cardFlat/cardBordered, rowTitleClass, transitionName ('aql-list-item'), dialogStyle ('max-width: 520px'), dialogTitleClass, dialogAddTitleLabel, dialogEditTitleLabel, addLabel/updateLabel/cancelLabel, addIcon/updateIcon/editIcon/deleteIcon/closeIcon, submitColor/cancelColor/editColor/deleteColor, actionBtnSize ('sm') }` | — | `FRONTENT/src/components/contents/FormChild.vue` |

## Action Subsystem (`src/components/actions/`)

Third placeholder paradigm alongside `Section.vue` and `Content.vue`. Base components here are mounted dynamically by `Action.vue` → `useActionResolver` and are overridable per tenant through the shared 10-tier `_ui/[UiName]/components/…` lookup (`[Action].vue` full override, `[Action].js` prop modifier).

> **Canonical spec** — complete prop tables, resolution internals, override cookbook, and the loading-UX contract: [AQL_ACTION_SYSTEM.md](file:///f:/LITTLE%20LEAP/AQL/Documents/AQL_ACTION_SYSTEM.md). Components in this folder carry **no `<style>` block**; all styling is `.aql-form-actions-*` / `.aql-crud-action-*` in `src/css/custom.scss`.

| Component Name | Description | Props | Events | Path |
|---|---|---|---|---|
| `Action` | Dynamic action placeholder mounted by `Page.vue` as `<Action action="PageAction" />`, after `AqlContentWrapper` and outside the animated `.aql-page-body` (a CSS transform on an ancestor would trap the fixed FAB). Resolves the matching `components/actions/*` component (plus tenant overrides/modifiers) through `useActionResolver`, showing a spinner while resolving and an "Action Not Defined" card when nothing matches. Action counterpart of `Section.vue` / `Content.vue`; `inheritAttrs: false`. | `{ action: String }` + all orchestrator `pageProps` via `$attrs` | — | `FRONTENT/src/components/Action.vue` |
| `ActionsPageAction` | Root action container. **Every** action — built-in or custom — routes through one dispatcher, `handleAction(actionName, extraPayload)`: it resolves a handler as `props[actionName]` (built-ins) or `$attrs[actionName]` (custom keys), awaits it with `(actionName, ctx)` where `ctx = { pageState, resourceConfig, resourceRecord, nav, payload }`, aborts on `false`/`{ valid:false, message }` (notifying `message`), normalizes an `Array` return to `{ requests }` and an `Object` return into the pageState call options, then runs the built-in default — `submit` → `pageState.submit()`/`run()` (→ `build`/`modifyPayload` → `onSuccess` → navigate), `reset` → `onReset()`, `cancel` → `nav.goBack()`. A custom key with no handler logs `[PageAction] No action handler supplied for: <key>`; with a handler it dispatches only when the result carried `requests`/`build`. Reset is a silent discard: re-seeds configured defaults on `add` via `initResource({ reset: true })`, re-hydrates the pristine server record on `edit`/`action` via `pageState.load()`. Mounts `FormActions` on add/edit/action and `CrudActions` everywhere else, **both via `useActionResolver`** so each is independently overridable; a `CrudActions` modifier returning `show:false`/`hide:true` suppresses the FAB cluster. `FormActions` deliberately receives no `$attrs` — `pageProps.onCancel` would bind as a `cancel` listener and double-navigate. | `{ page: String (required), scope/resource/uiName: String, actions: Array (null → FormActions default), submit/reset/cancel: Function (null), «customActionKey»: Function (via $attrs), modifyPayload: Function, successRoute: [String,Function], successMessage: [String,Function], onSubmitSuccess: Function, onSubmitError: Function, submitLabel: [String,Function] }` | — | `FRONTENT/src/components/actions/PageAction.vue` |
| `ActionsFormActions` | Sticky bottom form-actions bar. Owns only the chrome ("breeze" gradient — tinted left/empty side fading to soft white under the right-aligned buttons — top border, `blur(8px)` backdrop, 80px flow spacer against a `min-height: 64px` bar, 750ms-delayed slide-up entrance, reduced-motion aware). Bar and `.aql-form-actions-content` are both `display: flex; align-items: center` with `gap: 10px`, so buttons sit strictly on the vertical middle; Quasar's `q-gutter-*` is deliberately unused (its negative top margin fights that centering). Every button is mounted through `<Action>`, so each resolves independently at all 10 tiers. Which buttons render is driven by `actions`: a short key is prefixed/capitalised (`'submit'` → `FormActionSubmit`), an already-qualified name passes through, entries may be strings or `{ name, ...props }`. Unrecognised keys emit `action(key)`. Per-button prop merge: resolver context → per-key defaults → `actionProps[key]` → object-entry extras. | `{ actions: Array (['reset','submit']), page/scope/resource/uiName: String, submitLabel/resetLabel/cancelLabel: [String,Function] ('Save'/'Reset'/'Cancel'), submitIcon/resetIcon/cancelIcon: [String,Function] ('check'/'restart_alt'/'close'), submitColor/resetColor/cancelColor: [String,Function] ('primary'/'grey-7'/'grey-7'), disabled: [Boolean,Function] (submit-only gate), actionProps: Object }` | `submit()`, `reset()`, `cancel()`, `action(key)` | `FRONTENT/src/components/actions/FormActions.vue` |
| `ActionsFormActionSubmit` | Base submit button. Reports intent upward only — `PageAction.handleAction('submit')` dispatches. Renders as a `push glossy` `q-btn` carrying `.aql-form-action-btn` (padding `6px 18px`, `font-weight: 600`, `min-height: 40px`, 20px icon — all in `custom.scss`); `flat`/`unelevated` both default **false** because either would suppress the push/glossy shadow. Disabled (never spinner-loaded) while `pageState.meta.submitting` — the blocking indicator is `AqlContentWrapper`'s `q-inner-loading` overlay. Props typed `[String,Function]` and evaluated via `evaluateProp` (closures receive plain `(record, config)`). | `{ label: [String,Function] ('Save'), icon: [String,Function] ('check'), color: [String,Function] ('primary'), disabled: [Boolean,Function] (false), unelevated: Boolean (false), flat: Boolean (false) }` | `click(evt)` | `FRONTENT/src/components/actions/FormActionSubmit.vue` |
| `ActionsFormActionReset` | Base reset button. Same contract and `push glossy` / `.aql-form-action-btn` styling as Submit; subordination comes from `color` (`grey-7`), not flatness. Reset semantics live in `PageAction.onReset`. | `{ label: [String,Function] ('Reset'), icon: [String,Function] ('restart_alt'), color: [String,Function] ('grey-7'), disabled: [Boolean,Function] (false), flat: Boolean (false), unelevated: Boolean (false) }` | `click(evt)` | `FRONTENT/src/components/actions/FormActionReset.vue` |
| `ActionsFormActionCancel` | Base cancel button. Reports intent upward like Submit/Reset — navigation is `PageAction.handleAction('cancel')` → `nav.goBack()`, so a `cancel` handler returning `false` can veto it and no click ever pops two history entries. `cancelHandler` is a local escape hatch for standalone use outside PageAction: when supplied it **replaces** the emit so the container never also navigates. Named `cancelHandler`, **not** `onCancel` — Vue would treat the latter as an emit listener and silently bind `pageProps.onCancel`. Same `push glossy` / `.aql-form-action-btn` styling. | `{ label: [String,Function] ('Cancel'), icon: [String,Function] ('close'), color: [String,Function] ('grey-7'), disabled: [Boolean,Function] (false), flat: Boolean (false), unelevated: Boolean (false), cancelHandler: Function (null) }` | `click(evt)` | `FRONTENT/src/components/actions/FormActionCancel.vue` |
| `ActionsCrudActions` | CRUD floating action buttons with the 750ms-delayed `bounceIn` entrance (`.aql-crud-action-container`, applied to an inner wrapper — never to `q-page-sticky`, since a transform on a `position:fixed` ancestor becomes its containing block and breaks FAB positioning). Three mutually exclusive layouts: single Add FAB (`canWrite`), single Edit FAB (`canUpdate` + record), or the expandable `CrudActionsFab` menu (both). Never rendered on add/edit — `FormActions` owns those. Its FABs (`AddFab`/`EditFab`/`CrudActionsFab`) remain **Sections** under `components/sections/`, keep resolving via `useSectionResolver` (so existing tenant FAB overrides are untouched), and now render with `push glossy` styling for a premium, elevated appearance matching `FormActions` buttons. | `{ page: String ('index'), scope/resource/uiName: String }` | — | `FRONTENT/src/components/actions/CrudActions.vue` |
| `ActionsAddFab` | Round Add FAB (`glossy push`, `.aql-crud-action-fab`), or a `q-fab-action` when `asFabAction` is set so it participates in Quasar's stagger/auto-collapse inside `CrudActionsFab`. Relocated from `components/sections/` — it is a child of an action container, and the folder is the resolution contract (ARCHITECTURE RULES §8), so it now resolves via `useActionResolver`. The 10-tier `_ui/` override paths are unchanged. | `{ color: String ('primary'), icon: String ('add'), tooltip: String ('Add New'), asFabAction: Boolean }` | `click()` | `FRONTENT/src/components/actions/AddFab.vue` |
| `ActionsEditFab` | Round Edit FAB. Same shape, styling and relocation rationale as `ActionsAddFab`. | `{ color: String ('primary'), icon: String ('edit'), tooltip: String ('Edit'), asFabAction: Boolean }` | `click()` | `FRONTENT/src/components/actions/EditFab.vue` |
| `ActionsCrudActionsFab` | Expandable three-dot `q-fab` menu hosting the Add/Edit FABs via named slots (`AddFab`, `EditFab`, `RecordFab`, `ResourceFab`, `ViewFab`). Same relocation rationale as above. | — (slot host) | — | `FRONTENT/src/components/actions/CrudActionsFab.vue` |
| `ActionsResourceReports` | Report downloads as an action. Mounted by `PageAction` on every **non-form** page (bottom-left `q-page-sticky` FAB, so it never collides with the bottom-right CRUD cluster), by `FormActions` when `actions` contains `reports`, and directly anywhere as `<Action action="ResourceReports" mode="toolbar" />`. Context-adaptive: with a record in scope (explicit `record` prop, else injected `resourceRecord`) it lists `isRecordLevel` reports, without one it lists page-level reports. Four modes — `fab` (default) / `toolbar` (dropdown) / `card` (bordered bar) / `inline` (bare bar buttons). **Self-dispatching**: the one deliberate exception to "buttons never act on their own" — a download never touches `pageState`, so it calls `useReports` directly instead of emitting intent to `handleAction()`. All generation, dialog state, dynamic select preloading and notifications stay in `useReports`. Shares `CrudActions`' motion and elevation (`push glossy`, `.aql-report-action-fab` `@extend`s `.aql-crud-action-fab`) but **not** its shape or colour: a horizontal **pill** FAB (icon + short `label`, default `Reports`) in `teal-7` with white text. The pill geometry is Quasar's own for a labelled QFab — no CSS radius override. `card`/`inline` buttons carry `.aql-form-action-btn` + `.aql-report-action-btn`. The pill/teal treatment is exclusive to this component. `noReports: true` from pageProps suppresses only this cluster, unlike `noActions`. | `{ mode, record, reports, label, color, itemColor, textColor, icon, activeIcon, tooltip, position, offset, show, hide, noReports: [Type, Function], page/scope/resource/uiName: String }` | — (self-dispatching) | `FRONTENT/src/components/actions/ResourceReports.vue` |
| `AqlContentWrapper` | Content gate wrapping all `contents`. Five states (blocking spinner, non-blocking progress bar, record-not-found, empty dataset, normal slot) **plus** a submission overlay: `<q-inner-loading>` + `q-spinner-dots` across the content area whenever `pageState.meta.submitting`/`.saving` is true. Injects `pageState` directly, so pages need no wiring; `submitting` is an opt-in force flag. | `{ loading, empty, requiresRecord, recordExists, hasData, showBackToList, emptyIcon, emptyTitle, emptyMessage, notFoundTitle, notFoundMessage, submitting: Boolean (false), submittingLabel: String ('Saving…') }` | — | `FRONTENT/src/components/shared/AqlContentWrapper.vue` |

## Supporting Composables

| Composable | Description | Signature |
|---|---|---|
| `useListStrategy` | Derives `abstract/List.vue` label/caption/meta/highlight resolvers from a resource's headers, parent relations, and the active list view (`resourceRecord.activeView`). Label: own `Name` → first parent relation with a `Name` column (borrowed, e.g. SKU → Product, with `Variant1..5` appended) → 2+ descriptive columns combined (`"h2 - h3"`) → single descriptive column → `Code`. Caption: multiple parents (`relations.parents.length > 1`) join every parent's name/code with `•`; else a `Date`/`TransactionDate`/`CreatedAt` + `CreatedBy`/`UpdatedBy`/`User`/`Agent` pair; else whatever the label didn't consume. Highlight + meta-chip track `Progress` → `Status` → `Type`; while a list view is active that field is dropped from the chip (redundant with the selected view) in favor of a currency-formatted amount column (`_C` from `useCurrency`) or, for resources with >7 headers, a quantity column — highlight keeps tracking the state column regardless. Data-dense resources (>7 headers) also get a `metaCaption` from an unused descriptive column. Consumed by `contents/List.vue`. | `(resourceConfig, resourceRecord) => { layout, label, caption, metaLayout, chip, chipColor, chipOutline, metaLabel, metaCaption, highlight, highlightColor, clickable }` (all `ComputedRef`) — `FRONTENT/src/composables/resources/useListStrategy.js` |
| `useViewColumnResolver` | Resolves column-level custom UI overrides for fields rendered inside `ViewRecord`. Lookup uses the resource that OWNS the column (`_ui/{ui}/components/{scope}/{resourceSlug}/viewcolumn{col}.(vue\|js)`, then scope-wide, then ui-wide). Exports `resolveColumnOverride({ columnName, resourceSlug, scope, uiName }) => Promise<{ component, modifier }>` for batch resolution, plus the reactive `useViewColumnResolver(...)` composable for single-column use. Supports `.vue` SFC components, `.js` function modifiers, and `.js` object modifiers. | `useViewColumnResolver({ columnName, resourceSlug, scope, uiName }) => { resolvedComponent: ShallowRef, modifier: Ref }` — `FRONTENT/src/composables/resources/useViewColumnResolver.js` |
| `useListViews` | Manages list-view filter evaluation/counts/active state; when `configuredListViews` is empty and mode isn't `off`/`custom`, auto-generates views from `scope` + `resourceName`: `master` scope with a `Status` header → Active/Inactive; any other scope → the first of `Progress` header, `Type` header, or a header with a matching `<ResourceName><Header>` (or singularized) `AppOptions` group — values sourced from that `AppOptions` group when present, else the distinct values observed live. | `({ items, resourceHeaders, configuredListViews, configuredListViewsMode, scope?, resourceName?, enableUrlSync?, route?, router? }) => { effectiveViews, activeViewName, activeView, viewCounts, viewFilteredItems, setActiveView }` — `FRONTENT/src/composables/useListViews.js` |

