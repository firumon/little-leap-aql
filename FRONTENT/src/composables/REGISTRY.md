# Frontend Composable Registry

Purpose: quick discovery of reusable state/logic modules under `FRONTENT/src/composables/`.

Rule: update this file whenever a new composable is added or a listed composable's signature/returns change.

Design principle:
- Prefer tiny, single-responsibility composables with clear reuse intent.
- Avoid page-private one-off abstractions unless there is a clear reuse path.

| Composable Name | Description (What & Why) | Arguments | Returns | Path |
|---|---|---|---|---|
| `useMenuAccess` | Evaluates permission-based menu visibility rules. Supports single-resource checks (`require`), cross-resource `all` / `any` rules, and defaults to `canRead` on the current resource. | `()` | `{ evaluateMenuAccess }` | `FRONTENT/src/composables/layout/useMenuAccess.js` |
| `useMainLayoutNavTree` | Main layout sidebar/profile-menu view-model. Builds the visible grouped nav tree from auth resources and exposes drawer/logout handlers. | `()` | `{ leftDrawerOpen, userAvatar, userName, userRoleLabel, visibleResourceMenuGroups, toggleLeftDrawer, handleLogout }` | `FRONTENT/src/composables/layout/useMainLayoutNavTree.js` |
| `useLoginPage` | Login page orchestration for credential state, auth submission, success notification, and post-login navigation. | `()` | `{ loginForm, loading, handleLogin }` | `FRONTENT/src/composables/layout/useLoginPage.js` |
| `useProfilePage` | Profile page view-model for avatar/name/email/password dialogs and dashboard back navigation. | `()` | `{ profileData, showAvatarDialog, newAvatarUrl, updatingAvatar, showNameDialog, newName, updatingName, showEmailDialog, newEmail, updatingEmail, showPasswordDialog, currentPassword, newPassword, confirmPassword, updatingPassword, goBack, updateAvatar, openNameDialog, updateName, openEmailDialog, updateEmail, updatePassword }` | `FRONTENT/src/composables/layout/useProfilePage.js` |
| `useAppNav` | Small non-resource navigation helper for shared app-level routes. | `()` | `{ goTo, goToPath }` | `FRONTENT/src/composables/core/useAppNav.js` |
| `useAuthLogic` | Auth boundary composable wrapping auth-store login/logout/profile mutation workflows plus a reusable auth API call helper. | `()` | `{ login, logout, updateAvatar, updateName, updateEmail, updatePassword, callAuthApi }` | `FRONTENT/src/composables/core/useAuthLogic.js` |
| `useResourceConfig` | Resolves current resource configuration from route params + auth store. Exposes scope, slug, code, action context, headers, normalized fields/actions, and permissions. | `()` | `{ route, scope, resourceSlug, code, action, level, config, resourceName, customUIName, resourceHeaders, resolvedFields, additionalActions, permissions }` | `FRONTENT/src/composables/resources/useResourceConfig.js` |
| `useResourceNav` | Generic resource navigation helper that resolves current scope/resource/code, coerces route params to plain strings, supports cross-resource overrides, and forwards optional query values. | `()` | `{ goTo(target: String, params?: Object) }` | `FRONTENT/src/composables/resources/useResourceNav.js` |
| `useResourceData` | Cache-first data loading, filtering, and optimistic record updates for a resource via the Pinia data store. | `(resourceNameRef)` | `{ items, lastHeaders, filteredItems, loading, backgroundSyncing, searchTerm, showInactive, reload, reset, getRecordByCode, updateLocalRecord, notify }` | `FRONTENT/src/composables/resources/useResourceData.js` |
| `useResourceRelations` | Discovers parent/child resource relationships from the auth resource list and can build recursive trees. | `(resourceNameRef)` | `{ childResources, parentResource, hasChildren, hasParent, getChildResources, getResourceByName, buildResourceTree }` | `FRONTENT/src/composables/resources/useResourceRelations.js` |
| `useResourceRelationsData` | Loads parent and child records for a resource record so view pages stay free of relation-fetch orchestration. | `(resourceNameRef)` | `{ childResources, parentResource, childRecordsByResource, parentRecord, loadChildRecords, loadParentRecord, loadRelations }` | `FRONTENT/src/composables/resources/useResourceRelationsData.js` |
| `useResourceSync` | Sync orchestration facade over the sync store for queueing, flushing, and global/manual resource sync workflows with error tracking. | `()` | `{ isSyncing, lastSyncTime, syncErrors, syncableResources, syncResource, flushQueue, syncAllResources, syncResources, clearErrors }` | `FRONTENT/src/composables/resources/useResourceSync.js` |
| `useSectionResolver` | Generic 3-tier section resolver for master/operation page sections, honoring tenant custom, entity custom, then default components. | `({ resourceSlug, customUIName, sectionDefs, scope? })` | `{ sections, sectionsReady }` | `FRONTENT/src/composables/resources/useSectionResolver.js` |
| `useActionResolver` | Action-page section resolver that adds action-specific override tiers before falling back to entity/default sections. | `({ resourceSlug, customUIName, actionKey, sectionDefs, scope? })` | `{ sections, sectionsReady }` | `FRONTENT/src/composables/resources/useActionResolver.js` |
| `useActionFields` | Resolves action-form fields from normalized `additionalActions` config plus sheet headers, including outcome options and auto-fill columns. | `(resourceHeadersRef, actionConfigRef, selectedValueRef)` | `{ column, selectedValue, isMultiOutcome, outcomeOptions, resolvedFields, autoFillColumns, actionConfig }` | `FRONTENT/src/composables/resources/useActionFields.js` |
| `useCompositeForm` | Manages parent + child record groups for composite add/edit pages and saves them through the workflow store. | `(configRef)` | `{ parentForm, childGroups, isEdit, saving, originalCode, statusOptions, initializeForCreate, initializeForEdit, addChildRecord, removeChildRecord, updateChildField, save }` | `FRONTENT/src/composables/resources/useCompositeForm.js` |
| `useStockMovements` | Data layer behind the Stock Movements direct-entry flows. Loads warehouses/SKUs/storages and submits batched movement rows. | `()` | `{ loadWarehouses, loadSkusWithProducts, loadStoragesForWarehouse, submitBatch }` | `FRONTENT/src/composables/operations/stock/useStockMovements.js` |
| `useProcurements` | Procurement workflow helper for Purchase Requisition/RFQ stage transitions. Centralizes canonical progress values, editable/review/read-only mode detection, comment-thread formatting, and batch request builders for PR submit/review actions. | `()` | `{ purchaseRequisitionProgress, procurementProgress, progress, procurementStage, currentUserName, currentUserRole, currentTimestampLabel, appendWorkflowComment, formatWorkflowCommentHtml, isDraftProgress, isRevisionRequiredProgress, isPendingApprovalProgress, isEditableProgress, isReadOnlyProgress, resolveViewMode, buildEditableSubmitRequests, buildPendingApprovalRequests, buildProcurementCreateRequest, buildProcurementUpdateRequest }` | `FRONTENT/src/composables/operations/procurements/useProcurements.js` |
| `rfqMeta` | RFQ option metadata helpers with default app-option fallbacks and user-facing term descriptions for lead time, shipping, payment, validity, and delivery choices. | named exports | `{ RFQ_DESCRIPTIONS, mapRFQOptions, rfqFallbackValues, buildPrimaryMeta }` | `FRONTENT/src/composables/operations/rfqs/rfqMeta.js` |
| `rfqPayload` | RFQ payload/date helpers, including PR item CSV parsing/building and default RFQ date/submission deadline/lead-time calculations. | named exports | `{ toDateInputValue, addDays, daysUntil, buildPrItemCodeCsv, parsePrItemCodeCsv, buildRFQRecord }` | `FRONTENT/src/composables/operations/rfqs/rfqPayload.js` |
| `useRFQCreateFlow` | RFQ create wizard view-model. Lists approved PRs, shows included PR items, prepares RFQ terms, creates RFQ records, and updates linked PR/Procurement workflow state. | `()` | `{ steps, currentStep, loading, saving, searchTerm, form, optionSets, approvedPrs, selectedPr, selectedPrItems, calculatedLeadTimeDays, leadTimeChanged, canGoTiming, canConfirm, createdCode, formatDate, selectPr, goTo, loadData, confirmCreate }` | `FRONTENT/src/composables/operations/rfqs/useRFQCreateFlow.js` |
| `useRFQEditableFlow` | RFQ draft view/edit flow. Keeps RFQ/Procurement/PR/item link fields read-only, tracks dirty state, saves parent-only RFQ draft updates, and gates Assign Supplier / Mark As Sent navigation. | `()` | `{ nav, loading, saving, record, form, itemCodes, optionSets, isDraft, hasUnsavedChanges, canSave, canAssignSupplier, canMarkAsSent, resourceName, formatDate, loadData, saveDraft, assignSupplier, markAsSent, goToList }` | `FRONTENT/src/composables/operations/rfqs/useRFQEditableFlow.js` |
| `useRFQIndex` | RFQ list-page view-model that groups RFQs by progress, prioritizes Draft records, and handles search, refresh, and resource navigation. | `()` | `{ permissions, items, loading, reload, searchTerm, activeGroupKey, visibleGroups, totalVisible, isGroupExpanded, toggleGroup, formatDate, isOverdue, navigateTo, navigateToAdd }` | `FRONTENT/src/composables/operations/rfqs/useRFQIndex.js` |
| `useRFQSupplierFlow` | RFQ assign supplier and mark-as-sent flow view-model. Reads suppliers through the resource cache, builds master-backed supplier display rows, and batch-writes draft assignment, sent-RFQ supplier additions, and dispatch updates with procurement workflow advancement. | `(rfqCode)` | `{ isHeaderLoading, isSuppliersLoading, isSaving, rfqRecord, prRecord, headerDisplayFields, itemCodes, availableSuppliers, assignedSupplierRows, assignedSupplierDetails, isDraft, isSent, canAssignSupplier, canAddAvailableSuppliers, canMarkAsSent, loadRFQData, loadSuppliersData, loadData, saveAssignments, addAvailableSuppliers, markSelectedAsSent, assignSupplier, markAsSent, goToList, formatDate }` | `FRONTENT/src/composables/operations/rfqs/useRFQSupplierFlow.js` |
| `useRFQView` | Minimal RFQ read-only view-model that loads the selected RFQ and parses PR item CSV into display chips. | `()` | `{ loading, record, itemCodes, formatDate, goToList }` | `FRONTENT/src/composables/operations/rfqs/useRFQView.js` |
| `supplierQuotationMeta` | Supplier Quotation option/display metadata helpers for response/progress labels, charge keys, progress grouping, dates, and currency formatting. | named exports | `{ EXTRA_CHARGE_KEYS, RESPONSE_TYPES, PROGRESS_ORDER, labelFor, mapOptions, progressMeta, formatDate, formatCurrency }` | `FRONTENT/src/composables/operations/supplierQuotations/supplierQuotationMeta.js` |
| `supplierQuotationPayload` | Supplier Quotation payload, date, charge JSON, numeric normalization, item construction, and validation helpers for create/edit flows. | named exports | `{ toDateInputValue, addDays, normalizeNumber, normalizeFlag, flagValue, blankCharges, parseCharges, stringifyCharges, defaultHeaderForm, defaultItemForm, isQuotedItem, buildHeaderRecord, buildItemRecord, validateQuotation }` | `FRONTENT/src/composables/operations/supplierQuotations/supplierQuotationPayload.js` |
| `useSupplierQuotationIndex` | Supplier Quotation index view-model. Loads quotations/suppliers/procurements, filters stale rejected and accepted-completed records, groups by progress, and handles resource navigation. | `()` | `{ permissions, items, loading, searchTerm, groups, totalVisible, reload, isGroupExpanded, toggleGroup, navigateTo, navigateToAdd, supplierName, formatDate, formatCurrency }` | `FRONTENT/src/composables/operations/supplierQuotations/useSupplierQuotationIndex.js` |
| `useSupplierQuotationCreateFlow` | Supplier Quotation add-page flow. Selects sent RFQs and assigned suppliers, warns on duplicate supplier responses, builds quoted/partial/declined payloads, and batch-saves first-response workflow updates. | `()` | `{ loading, saving, rfqSearch, selectedRfqCode, selectedSupplierCode, form, items, optionSets, sentRfqs, selectedRfq, assignedSuppliers, selectedSupplier, existingSupplierQuotations, itemContext, itemSubtotal, extraChargesTotal, suggestedTotal, canSave, loadData, selectRfq, selectSupplier, save, cancel, formatDate, formatCurrency }` | `FRONTENT/src/composables/operations/supplierQuotations/useSupplierQuotationCreateFlow.js` |
| `useSupplierQuotationView` | Supplier Quotation view/edit flow. Loads header/items/context, gates editable behavior to `RECEIVED`, saves existing quotation edits without repeating workflow updates, and executes Reject through AdditionalActions. | `()` | `{ loading, saving, rejecting, rejectComment, record, form, items, rfq, supplier, progress, isEditable, isReadonly, optionSets, itemSubtotal, canSave, canReject, loadData, save, reject, goToList, formatDate, formatCurrency }` | `FRONTENT/src/composables/operations/supplierQuotations/useSupplierQuotationView.js` |
| `usePurchaseRequisitionCreateFlow` | PR initiate-flow view-model for setup step state, SKU catalog loading, stock-aware item selection, and draft PR creation. | `()` | `{ steps, currentStep, loadingWarehouses, warehouses, form, types, priorities, selectedType, needsRefCode, isStep1Valid, goToStep2, loadingItems, products, skus, stockData, searchQuery, itemSort, showAllWh, saving, sortedItems, selectedSkusCount, totalQty, warehouseName, today, todayFormatted, createdCode, getSkuStock, stockClass, viewCreatedPR, savePR }` | `FRONTENT/src/composables/operations/purchaseRequisitions/usePurchaseRequisitionCreateFlow.js` |
| `usePurchaseRequisitionEditableFlow` | Editable PR flow for both `Draft` and `Revision Required`. Tracks dirty state, gates `Update PR` vs `Submit PR`, enforces revision-update comments, and handles inline item editing plus submit/resubmit through `useProcurements`. | `()` | `{ nav, loading, saving, submitting, prForm, items, deletedItemCodes, responseComment, warehouses, loadingWarehouses, skuInfoByCode, skuOptions, itemSearch, addDialog, newItem, focusedField, types, priorities, isRevision, revisionThread, rejectedComment, filteredItems, totalQty, grandTotal, selectedWarehouse, needsRefCode, hasUnsavedChanges, updateCommentRequired, canUpdate, canSubmit, itemColumns, statusChipStyle, formatDate, formatCurrency, openAddItemDialog, filterSkus, confirmAddItem, removeItem, updatePr, saveDraft, submit, buildPayload, loadData }` | `FRONTENT/src/composables/operations/purchaseRequisitions/usePurchaseRequisitionEditableFlow.js` |
| `usePurchaseRequisitionDraftFlow` | Compatibility alias to `usePurchaseRequisitionEditableFlow` for older draft-page imports. | `()` | `same as usePurchaseRequisitionEditableFlow` | `FRONTENT/src/composables/operations/purchaseRequisitions/usePurchaseRequisitionDraftFlow.js` |
| `usePurchaseRequisitionIndex` | PR list-page view-model that groups records by workflow stage and enforces one expanded group at a time with initial focus on `Revision Required`, else recent `Rejected`, else all collapsed. | `()` | `{ permissions, items, loading, reload, searchTerm, activeGroupKey, visibleGroups, totalVisible, priorityColor, formatDate, isOverdue, isGroupExpanded, toggleGroup, navigateTo, navigateToAdd }` | `FRONTENT/src/composables/operations/purchaseRequisitions/usePurchaseRequisitionIndex.js` |
| `usePurchaseRequisitionApprovalFlow` | Pending-approval PR review flow. Loads PR/item data and executes Approve, Reject, and Send Back transitions through `useProcurements`. | `()` | `{ nav, loading, acting, prForm, items, warehouses, selectedWarehouse, actionComment, totalQty, grandTotal, formatDate, formatCurrency, statusChipStyle, loadData, approve, reject, sendBack }` | `FRONTENT/src/composables/operations/purchaseRequisitions/usePurchaseRequisitionApprovalFlow.js` |
| `usePurchaseRequisitionReviewFlow` | Compatibility re-export of `usePurchaseRequisitionEditableFlow` for older review-page imports. | `()` | `same as usePurchaseRequisitionEditableFlow` | `FRONTENT/src/composables/operations/purchaseRequisitions/usePurchaseRequisitionReviewFlow.js` |
| `useProductVariants` | Product variant helper for parsing `VariantTypes`, building variant columns, and validating duplicate/required SKU variant sets. | `(productOrRef)` | `{ variantColumns }` plus named exports `parseVariantTypes`, `hasDuplicateVariantSet`, `validateSkuVariants` | `FRONTENT/src/composables/masters/products/useProductVariants.js` |
| `useProductCreateForm` | Product create-page view-model that coordinates the parent form, dynamic SKU child rows, variant-type editing, and save navigation. | `()` | `{ parentForm, saving, statusOptions, newVariantType, variantTypeList, variantColumns, skuRecords, skuColumns, duplicateIssue, addVariantType, removeVariantType, addSkuRow, removeSkuRow, restoreSkuRow, updateSkuField, isRowInactive, handleSave, navigateBack }` | `FRONTENT/src/composables/masters/products/useProductCreateForm.js` |
| `useProductEditForm` | Product edit-page view-model that loads the product/SKU bundle, manages variant edits, and performs optimistic local updates after save. | `()` | `{ code, parentForm, saving, statusOptions, newVariantType, record, variantColumns, skuRecords, variantTypeList, duplicateIssue, skuColumns, loading, addVariantType, confirmRemoveVariantType, addSkuRow, removeSkuRow, restoreSkuRow, updateSkuField, isRowInactive, handleSave, navigateBack, navigateToList }` | `FRONTENT/src/composables/masters/products/useProductEditForm.js` |
| `useProductSkuViewData` | Product detail helper that loads SKU child rows with cache-first behavior and optional background refresh. | `()` | `{ skuRows, skuLoading, loadSkuRows }` | `FRONTENT/src/composables/masters/products/useProductSkuViewData.js` |
| `useReports` | Report generation orchestration that resolves report configs, collects optional dialog inputs, builds cell payloads, and downloads generated PDFs. | `(resourceNameRef)` | `{ isGenerating, showReportDialog, activeReport, reportInputs, getToolbarReports, getRecordReports, requiresUserInput, initiateReport, confirmReportDialog, cancelReportDialog, executeReport }` | `FRONTENT/src/composables/reports/useReports.js` |
| `useBulkUpload` | Shared logic for bulk upload flows: resource/header selection, draft restore/save, table plotting, inline edits, and upload execution. | `()` | `{ selectedResourceName, rawContent, csvFile, headersDisplay, rows, isUploading, resourceOptions, selectedResourceHeaders, activeHeaders, columns, onResourceSelected, downloadTemplate, handleFileUpload, plotTable, onCellEdited, deleteRow, saveDraft, clearAll, uploadAll }` | `FRONTENT/src/composables/upload/useBulkUpload.js` |
| `useListViews` | Generic list view filtering. Evaluates filter trees from `APP.Resources.ListViews` JSON, supports `listViewsMode` (`auto`/`off`/`custom`), auto-generates Active/Inactive only in auto mode with `Status` header, computes per-view counts, and manages active view state. URL sync is optional via `enableUrlSync`. | `({ items, resourceHeaders, configuredListViews, configuredListViewsMode?, enableUrlSync?, route?, router? })` | `{ effectiveViews, activeViewName, activeView, viewCounts, viewFilteredItems, setActiveView }` | `FRONTENT/src/composables/useListViews.js` |
| `useDashboardWidgets` | Dashboard page view-model that resolves profile greeting state and widget summaries without pages importing stores directly. | `()` | `{ userName, widgetCards }` | `FRONTENT/src/composables/useDashboardWidgets.js` |
| `useMasterActions` | Boundary composable for master additional-action pages. Loads the target record and submits action payloads through approved store/service wrappers. | `()` | `{ loading, saving, config, resourceName, code, action, actionConfig, record, resourceHeaders, actionFields, actionForm, primaryText, submitAction, goBack }` | `FRONTENT/src/composables/useMasterActions.js` |
| `useOperationActions` | Boundary composable for operations additional-action pages. Mirrors `useMasterActions` with operation-scope routing and payload execution. | `()` | `{ loading, saving, config, resourceName, code, action, actionConfig, record, resourceHeaders, actionFields, actionForm, primaryText, submitAction, goBack }` | `FRONTENT/src/composables/useOperationActions.js` |
